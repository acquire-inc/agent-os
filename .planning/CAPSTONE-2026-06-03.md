# Capstone — Phases 11-22 (2026-06-03)

Single session, 12 phases shipped, all on `claude/exciting-davinci-yvptm` (pushed). AgentOS-only scope throughout (no Cliently work). 0 T-critical seed touches. All tests green at every commit.

## What landed

| Phase | Summary | Commit |
|---|---|---|
| 11 | External runtime optimization: read-only audit + quarantined extraction + operator decisions recorded | `251f142`, `ef5a4c4`, `d7a74b5`, `135733d` |
| 12 | AgentOS safety regressions: `agents.key` immutability test + router↔runner allowlist parity test (65 new assertions) | `cde191e` |
| 13 | Candidate ATTACH (5 of 8): reauthored 5 SKILL.md files + 69 attach operations across 44 agent seeds (0 T-critical touches) | `b7eab46` |
| 14 | CRA prohibition blocklist (HARD GATE): `cra-blocklist.ts` + hydrate refusal + runtime SessionStart guard emitting `cantfail.cra_violation` | `d01102c` |
| 15 | Prompt-injection guard runtime: `injection-guard.ts` (6 categories × multi-pattern) wired into `tool.browser` | `3d8cea6` |
| 16 | Budget reserve/commit/release: `BudgetTracker` class + 5 new `budget.*` Relay event names | `17ac68b` |
| 17 | Runner BudgetTracker integration: `executeRun` opens/closes the budget lifecycle on every exit path | `9746c94` |
| 18 | Agent evaluator scorecard (pure): `scoreAgent()` with 5 verdicts × 5 demote thresholds × 3 promote bars | `427b5a8` |
| 19 | Relay emission wiring: BudgetEvent → `budget.*` + injection detections → `finding.recorded` | `47cb6d8` |
| 20 | Scorecard controller + `agent_scorecards` migration: `computeNextAutonomy` + migration 0014 + Drizzle table | `ab373eb` |
| 21 | `setAutonomy` lifecycle function + `runScorecardJob` orchestrator | `e480c4d` |
| 22 | Per-run autonomy ratchet on injection detection — closes Phase 13 SKILL workflow step 5 | `f6bad35` |

## Test footprint (new + maintained green)

**`@agent-os/core` test suite (10 named tests, 338 assertions):**

| Test | Phase | Assertions |
|---|---|---|
| `test:immutability` | 12 | 61 |
| `test:parity` | 12 | 4 |
| `test:cantfail` | (existing) | 10 |
| `test:architect` | (existing) | 37 |
| `test:cra` | 14 | 36 |
| `test:injection` | 15 | 58 |
| `test:budget` | 16 | 49 |
| `test:scorecard` | 18 | 33 |
| `test:controller` | 20 | 25 |
| `test:job` | 21 | 25 |

**`@agent-os/runner` test suite (4 named tests, 43 assertions):**

| Test | Phase | Assertions |
|---|---|---|
| `test` (custom-tools) | (existing) | 6 |
| `test:cantfail` | (existing) | 7 |
| `test:budget` | 17 | 11 |
| `test:run-state` | 22 | 19 |

**`@agent-os/db` typecheck** clean across the new `agentScorecards` table.

## Doctrine claims that moved from "aspirational" to "load-bearing"

1. **"Promotion is earned from eval/approval-rate metrics; demotion is automatic on drops."** Phase 18 (scoring) + Phase 20 (controller + persistence) + Phase 21 (mutation + orchestration) make this end-to-end code-complete. The remaining piece is the Inngest scheduled trigger that walks each agent — a thin wrapper around `runScorecardJob`.

2. **"On any match, autonomy ratchets down to propose for the rest of the run."** Phase 13 SKILL → Phase 15 detection → Phase 19 audit → Phase 22 enforcement. The runner's PreToolUse gate now reads the per-run override on every tool call.

3. **"Architect MUST refuse CRA-touching blueprints. Runtime guard MUST refuse manually-authored seeds that bypassed it."** Phase 14. Both enforcement points emit Relay events; global invariant; no per-tenant override.

4. **"Every spend goes through reserve → commit (success) or release (failure). Cap breach refuses the next reserve."** Phase 16 (tracker) + Phase 17 (integration) + Phase 19 (Relay emit).

5. **"agents.key passes through hydration byte-for-byte; T_CRITICAL_ALLOWLIST ↔ runner allowlist must match."** Phase 12 regression tests fail loud at CI time if either invariant drifts.

## Scope-fence honored

- Zero T-critical seed touches across all 12 phases (verified by Phase 12 immutability test + Phase 13 verify pass)
- Zero `CLAUDE.md` edits (current-phase line update in Phase 19+20 housekeeping is the only one)
- Zero `cliently.dev` references in any new code (Phase 11 decision: Cliently is out of AgentOS scope)
- Zero `tier-models.ts` changes (Hermes Path A held in Phase 11)
- 5 of 8 quarantined candidates attached; 3 KEEP-FOR-LATER per Phase 11 decisions

## Pending operator gates (deferred — need external access)

- `supabase db push` for migrations 0010-0014 (Phase 9 + Phase 20)
- `pnpm seed:phase-9` (Phase 9 isolation tester seed)
- `pnpm verify:isolation-live` (Phase 9 HARD GATE #2 check)
- Inngest scheduled function wrapping `runScorecardJob` (needs Inngest dev access)
- Wave 2 quarantine branch `feat/external-skills-extraction @ 3eb2bff` push (intermittent upstream 503 — retry succeeds eventually)

## What's left for future phases

**Tier 2 backlog (recorded in `ROADMAP.md`):**
- Inngest function wrapping `runScorecardJob` for each active agent on a cron
- Operator dashboard view over `agent_scorecards` + `agent_scorecards_xtenant_agg`
- Cap-breach Approval surfacing (the Phase 13 cost-ceiling-discipline workflow step 5)
- Stagehand backend implementation for `tool.browser` (external service)
- DB-backed `BudgetTracker` persistence so reserves survive runner restart
- Per-tool reserve/commit using per-tool cost estimate column
- Cross-tenant scorecard aggregation (moat lens — read from the view)
- Per-tenant threshold overrides (agent-onboarder tight cycle)

**Doctrine still aspirational (not gated by code yet):**
- `secret-scan-veto` skill has no AgentOS attach target until a code-writing AgentOS agent exists
- 13 deferred deterministic tools beyond the current `tool.browser` / `tool.rls-test` / `tool.vault-rotate` / `tool.access-audit` / `tool.access-log-analyzer` set

## Final state

- Branch: `claude/exciting-davinci-yvptm`
- Last commit: `f6bad35`
- ROADMAP.md: Phases 11-19 marked complete; Tier 2 backlog section added
- CLAUDE.md: current-phase line updated
- All tests green at HEAD: 338 + 43 + db typecheck = 381+ assertions

The eval-driven autonomy ladder is now load-bearing code. Safety floors (cant-fail, CRA, injection guard, autonomy ratchet) all enforced. Budget tracking lifecycle is real. The seed scripts have the discipline skills attached. AgentOS hardening cycle done.
