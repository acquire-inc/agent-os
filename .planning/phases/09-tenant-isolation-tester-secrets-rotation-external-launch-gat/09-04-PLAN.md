---
phase: 09-tenant-isolation-tester-secrets-rotation-external-launch-gat
plan: 04
type: execute
wave: 3
depends_on: ["09-02", "09-03"]
files_modified:
  - scripts/seed/acqu-tool-rls-test.ts
  - scripts/seed/acqu-tool-vault-rotate.ts
  - scripts/seed/acqu-tool-access-audit.ts
  - scripts/seed/acqu-tool-access-log-analyzer.ts
  - scripts/seed/seed-tool-rls-test.ts
  - scripts/seed/seed-tool-vault-rotate.ts
  - scripts/seed/seed-tool-access-audit.ts
  - scripts/seed/seed-tool-access-log-analyzer.ts
  - apps/runner/src/custom-tools.ts
  - apps/runner/package.json
autonomous: true
requirements: [SC-9-2]
must_haves:
  truths:
    - "4 tool registry rows seed idempotently via ensureTool (tool.rls-test, tool.vault-rotate, tool.access-audit, tool.access-log-analyzer)"
    - "tool.vault-rotate has requiresApproval=true (D-03 — propose-gated for client OAuth per v2 D5.3 L1254); other 3 are requiresApproval=false (read-only)"
    - "customToolDispatch in apps/runner registers 4 new handlers; each returns { result } and the framework writes to tmpdir (CLAUDE.md non-negotiable #4 honored for free)"
    - "Runner imports compile and typecheck against the new @agent-os/tool-rls-test + @agent-os/core security exports"
  artifacts:
    - path: scripts/seed/acqu-tool-rls-test.ts
      provides: "tool.rls-test registry row + standalone runner"
      contains: "tool.rls-test"
    - path: scripts/seed/acqu-tool-vault-rotate.ts
      provides: "tool.vault-rotate registry row (requiresApproval=true)"
      contains: "requiresApproval: true"
    - path: apps/runner/src/custom-tools.ts
      provides: "4 new entries in customToolDispatch"
      contains: "tool.rls-test"
  key_links:
    - from: apps/runner/src/custom-tools.ts
      to: "@agent-os/tool-rls-test"
      via: "import { runIsolationSuite }"
      pattern: "from \"@agent-os/tool-rls-test\""
    - from: apps/runner/src/custom-tools.ts
      to: "@agent-os/core"
      via: "import { rotateCredential, findOrphanedGrants, detectUsageSpikes }"
      pattern: "from \"@agent-os/core\""
---

<objective>
Wire the 3 packages from Waves 1-2 into the runtime: 4 tool registry seed scripts (one per tool, plus standalone runners) + 4 new entries in `customToolDispatch`. This is the bridge between the deterministic tool layer (built in 09-02 + 09-03) and the agents (seeded in 09-05).

Purpose: per Pattern S7/S8 + the `tool.browser` precedent (Phase 7) — every custom tool registers via `ensureTool(db, tenantId, { key, name, kind: "custom", inputSchema, requiresApproval, reversible })` so the agent's tool binding (in 09-05 spec) can resolve at runtime; the runner's `customToolDispatch` map is what `dispatchCustomTool` consults to execute it.

Output: 8 new seed-related files (4 spec + 4 standalone runners) + extended `custom-tools.ts` dispatch map.
</objective>

<execution_context>
@/home/user/agent-os/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/agent-os/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/user/agent-os/.planning/phases/09-tenant-isolation-tester-secrets-rotation-external-launch-gat/09-RESEARCH.md
@/home/user/agent-os/.planning/phases/09-tenant-isolation-tester-secrets-rotation-external-launch-gat/09-PATTERNS.md
@/home/user/agent-os/.planning/phases/09-tenant-isolation-tester-secrets-rotation-external-launch-gat/09-CONTEXT.md
@/home/user/agent-os/.planning/phases/09-tenant-isolation-tester-secrets-rotation-external-launch-gat/09-02-SUMMARY.md
@/home/user/agent-os/.planning/phases/09-tenant-isolation-tester-secrets-rotation-external-launch-gat/09-03-SUMMARY.md

<interfaces>
<!-- Existing shapes the executor depends on -->

From scripts/seed/acqu-tool-browser.ts (55 lines — the verbatim per-tool seed template per PATTERNS.md S7):
- Imports: `createDb, schema` from `@agent-os/db`; `ensureTool` from `@agent-os/core`; `TENANT_IDS` from `@agent-os/shared`; `eq` from `drizzle-orm`
- INPUT_SCHEMA: hand-written JSON Schema mirroring the tool's zod input
- `ensureTool(db, TENANT_ID, { key, name, kind: "custom", description, inputSchema, requiresApproval, reversible })`
- Idempotent (uses upsert semantics inside ensureTool)
- Standalone runner if-block at bottom: `if (import.meta.url === ...) void main();`

From scripts/seed/seed-tool-browser.ts (the separate "seed" file that just calls the spec):
- One-line file: imports the spec, runs main, exits

From apps/runner/src/custom-tools.ts (lines 23-65 — the dispatch map + wrapper):
- `export interface CustomToolHandler { (input: unknown, ctx: { tenantId, outputDir, ... }): Promise<{ result: unknown }> }`
- `export const customToolDispatch: Record<string, CustomToolHandler> = { "tool.browser": async (input, ctx) => {...} }`
- `dispatchCustomTool(key, input, ctx)` wrapper (lines 47-65) writes result to `${ctx.outputDir}/${key}-result.json` — handler just returns `{ result }`

From @agent-os/tool-rls-test (plan 09-02):
- `runIsolationSuite(input: IsolationInput, opts?: { outputDir?: string; db?: Db }): Promise<IsolationResult>`
- IsolationInput: `{ tenantPairs: Array<{ userA, userB, tenantA, tenantB }>; tables?: string[] }`

From @agent-os/core security re-exports (plan 09-03):
- `rotateCredential(db, key, mcpId, refresher): Promise<{ rotated, reason? }>`
- `findOrphanedGrants(db, tenantId): Promise<Row[]>`
- `detectUsageSpikes(db, tenantId, window?): Promise<Row[]>`

From the package.json scripts file (scripts/seed/package.json):
- Existing pattern: `"tool-browser": "tsx seed-tool-browser.ts"` — add 4 analogous entries
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 4 tool registry seeds + 4 standalone runners + scripts/seed/package.json entries</name>
  <files>scripts/seed/acqu-tool-rls-test.ts, scripts/seed/acqu-tool-vault-rotate.ts, scripts/seed/acqu-tool-access-audit.ts, scripts/seed/acqu-tool-access-log-analyzer.ts, scripts/seed/seed-tool-rls-test.ts, scripts/seed/seed-tool-vault-rotate.ts, scripts/seed/seed-tool-access-audit.ts, scripts/seed/seed-tool-access-log-analyzer.ts, scripts/seed/package.json</files>
  <read_first>
    - scripts/seed/acqu-tool-browser.ts (55 lines — the verbatim template per PATTERNS.md S7; copy structure, swap key/name/description/inputSchema/requiresApproval)
    - scripts/seed/seed-tool-browser.ts (the separate runner file)
    - scripts/seed/package.json (current scripts — add 4 new `pnpm seed:tool-*` entries)
    - .planning/phases/09-tenant-isolation-tester-secrets-rotation-external-launch-gat/09-PATTERNS.md lines 153-203 (per-tool deltas table — defines requiresApproval, reversible, description per tool)
    - packages/tool-rls-test/src/types.ts (the IsolationInputSchema zod shape — mirror as JSON Schema in INPUT_SCHEMA constant)
  </read_first>
  <action>
    Copy `scripts/seed/acqu-tool-browser.ts` 4 times — once per new tool. For each, change ONLY the constants per the per-tool deltas table (PATTERNS.md lines 196-202):

    | Filename | tool key | name | requiresApproval | reversible | description |
    |---|---|---|---|---|---|
    | acqu-tool-rls-test.ts | tool.rls-test | RLS Test Suite | false | true | Cross-tenant query attack suite with append-only vector registry and positive controls |
    | acqu-tool-vault-rotate.ts | tool.vault-rotate | Vault Rotate | **true** | false | OAuth credential rotation — propose-gated per v2 D5.3 L1254 (client creds never rotate unilaterally) |
    | acqu-tool-access-audit.ts | tool.access-audit | Access Audit | false | true | Read-only orphan-grant inventory (filters archived agents >30d per Pitfall 4) |
    | acqu-tool-access-log-analyzer.ts | tool.access-log-analyzer | Access Log Analyzer | false | true | Read-only 24h-rolling usage anomaly scan over audit_log + autonomy_events |

    INPUT_SCHEMA per tool (hand-write JSON Schema mirroring each module's zod input — see packages/tool-rls-test/src/types.ts for tool.rls-test; for the 3 core modules, the schema is `{ type: "object", properties: { tenantId: { type: "string", format: "uuid" }, ...optional knobs }, required: ["tenantId"] }`).

    For tool.vault-rotate `requiresApproval: true` is the CRITICAL invariant — D-03 enforces propose-gating for client OAuth rotation. PR reviewers should reject any change flipping this to false. Cite v2 D5.3 line 1254 in a comment above the field.

    Create the 4 sibling `seed-tool-*.ts` files following `seed-tool-browser.ts` shape — each is a thin wrapper that calls the spec's exported `seed*` function and exits.

    Add 4 entries to `scripts/seed/package.json` scripts:
    ```
    "tool-rls-test": "tsx seed-tool-rls-test.ts",
    "tool-vault-rotate": "tsx seed-tool-vault-rotate.ts",
    "tool-access-audit": "tsx seed-tool-access-audit.ts",
    "tool-access-log-analyzer": "tsx seed-tool-access-log-analyzer.ts",
    ```
  </action>
  <verify>
    <automated>pnpm --filter @agent-os/seed typecheck</automated>
    <automated>node -e "const a = require('fs').readFileSync('scripts/seed/acqu-tool-vault-rotate.ts','utf8'); if (!/requiresApproval:\s*true/.test(a)) { console.error('FAIL: tool-vault-rotate must be propose-gated per D-03'); process.exit(1); } console.log('OK')"</automated>
    <automated>for f in scripts/seed/acqu-tool-rls-test.ts scripts/seed/acqu-tool-vault-rotate.ts scripts/seed/acqu-tool-access-audit.ts scripts/seed/acqu-tool-access-log-analyzer.ts; do grep -q 'ensureTool' "$f" || { echo "MISSING ensureTool in $f"; exit 1; }; done; echo OK</automated>
    <automated>node -e "const p = JSON.parse(require('fs').readFileSync('scripts/seed/package.json','utf8')); const need = ['tool-rls-test','tool-vault-rotate','tool-access-audit','tool-access-log-analyzer']; for (const k of need) { if (!p.scripts[k]) { console.error('missing script:', k); process.exit(1); } } console.log('OK')"</automated>
  </verify>
  <done>All 4 tool seed scripts + 4 standalone runners exist, typecheck, follow the tool-browser pattern; tool.vault-rotate has requiresApproval=true; package.json scripts wired.</done>
</task>

<task type="auto">
  <name>Task 2: Extend apps/runner customToolDispatch with 4 new handlers</name>
  <files>apps/runner/src/custom-tools.ts, apps/runner/package.json</files>
  <read_first>
    - apps/runner/src/custom-tools.ts (full file — the existing dispatch map shape per PATTERNS.md lines 601-634)
    - apps/runner/package.json (current dependencies — add @agent-os/tool-rls-test workspace dep)
    - packages/tool-rls-test/src/index.ts (from 09-02 — the IsolationInput + IsolationResult + runIsolationSuite exports)
    - packages/core/src/index.ts (from 09-03 — rotateCredential + findOrphanedGrants + detectUsageSpikes exports)
    - .planning/phases/09-tenant-isolation-tester-secrets-rotation-external-launch-gat/09-PATTERNS.md lines 601-634 (the extension skeleton with all 4 entries)
  </read_first>
  <action>
    Add `@agent-os/tool-rls-test: "workspace:*"` to apps/runner/package.json dependencies (next to existing `@agent-os/tool-browser` workspace dep — same alphabetical block).

    Extend `apps/runner/src/custom-tools.ts` per PATTERNS.md lines 605-631:
    - Add imports at top: `import { runIsolationSuite, type IsolationInput } from "@agent-os/tool-rls-test";` and `import { rotateCredential, findOrphanedGrants, detectUsageSpikes, closeRefresher, metaRefresher, stripeRefresher, type RotateResult } from "@agent-os/core";`
    - For vault key: import from `@agent-os/vault` (already wired in runner — reuse the existing pattern from `tool.browser`).
    - Add 4 new entries to `customToolDispatch`, preserving the existing `tool.browser` entry. Each handler:
      - `tool.rls-test`: parse input as IsolationInput; build the right db (use `ctx.db` if already wired, else accept a fallback — but for this dispatcher, the runner-provided `ctx.db` is the service-role connection which would invalidate the test. Document the issue inline: this handler is for in-runner self-test ONLY; the hard-gate live verification uses `scripts/verify/isolation-live.ts` (plan 09-06) which opens its own `RLS_TEST_DATABASE_URL` connection. The runner-dispatched call is for cron-driven sanity, not the hard gate. Per D-01.); call `runIsolationSuite(input, { outputDir: ctx.outputDir, db: ctx.db })`.
      - `tool.vault-rotate`: parse `{ mcpId, provider }`; resolve refresher by provider name (`close` → closeRefresher, `meta` → metaRefresher, `stripe` → stripeRefresher); call `rotateCredential(ctx.db, vaultKey, mcpId, refresher)`.
      - `tool.access-audit`: parse `{ tenantId }`; call `findOrphanedGrants(ctx.db, ctx.tenantId ?? input.tenantId)`.
      - `tool.access-log-analyzer`: parse `{ tenantId, hours? }`; call `detectUsageSpikes(ctx.db, ctx.tenantId, { hours: input.hours ?? 24 })`.

    Each handler returns `{ result }`. The framework's `dispatchCustomTool` wrapper (existing lines 47-65) writes the result to `${ctx.outputDir}/${toolKey}-result.json` — no need to write a file inside the handler. CLAUDE.md non-negotiable #4 satisfied for free.

    Add a one-line citation at the `tool.rls-test` handler: `// D-01: runner-dispatched RLS test uses ctx.db (likely service-role) — for cron sanity only; HARD GATE verification uses scripts/verify/isolation-live.ts with RLS_TEST_DATABASE_URL`.
  </action>
  <verify>
    <automated>pnpm --filter @agent-os/runner typecheck</automated>
    <automated>node -e "const c = require('fs').readFileSync('apps/runner/src/custom-tools.ts','utf8'); for (const k of ['tool.rls-test','tool.vault-rotate','tool.access-audit','tool.access-log-analyzer']) { if (!c.includes(k)) { console.error('missing handler:', k); process.exit(1); } } if (!c.includes('@agent-os/tool-rls-test')) { console.error('missing import @agent-os/tool-rls-test'); process.exit(1); } if (!/D-01/.test(c)) { console.error('missing D-01 citation on tool.rls-test handler'); process.exit(1); } console.log('OK')"</automated>
    <automated>pnpm -r typecheck 2>&1 | tail -10</automated>
  </verify>
  <done>Runner typechecks; all 4 handlers present in customToolDispatch; D-01 citation present at tool.rls-test; full workspace -r typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent prompt → tool invocation | Runner consults `customToolDispatch` by key; agents cannot inject arbitrary keys (registry table is the source of truth via 09-04 seeds) |
| service-role runner connection → RLS test | D-01: in-runner dispatch is sanity-only; HARD GATE uses separate connection in 09-06 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-01 | Information Disclosure | RLS test false-pass via service-role | mitigate | D-01 citation inline on the handler; HARD GATE runs out-of-runner with RLS_TEST_DATABASE_URL (plan 09-06). In-runner tool serves agent-driven sanity checks where false-pass is detected via positive controls in attack-vectors.ts (Pitfall 5). |
| T-9-14 | Elevation of Privilege | vault rotation called without approval | mitigate | tool.vault-rotate `requiresApproval: true` → PreToolUse approval hook (Phase 1, hook 1c) gates dispatch. Even if agent prompt requests rotation, runner pauses for human approval. |
| T-9-15 | Tampering | dispatch map mutated to swap handler bodies | accept | dispatch map is closed-over module-scope const; only PR-time tampering possible. Code review + git history audit. |
| T-9-SC | Tampering | new workspace dep | mitigate | Only `@agent-os/tool-rls-test` workspace ref added — internal package from 09-02. No external installs. |
</threat_model>

<verification>
- `pnpm --filter @agent-os/runner typecheck` green
- `pnpm -r typecheck` green (no cross-package break)
- All 4 tool keys present in apps/runner/src/custom-tools.ts
- tool.vault-rotate seed has requiresApproval=true (D-03 invariant)
- D-01 citation present at tool.rls-test handler in custom-tools.ts
- 4 new `pnpm seed:tool-*` scripts wired in scripts/seed/package.json
</verification>

<success_criteria>
- SC-9-2 complete: 3 new deterministic tools (vault-rotate, access-audit, access-log-analyzer) + the RLS test tool registered + runner-dispatched. Tool→agent binding happens in 09-05 seeds.
</success_criteria>

<output>
Create `.planning/phases/09-tenant-isolation-tester-secrets-rotation-external-launch-gat/09-04-SUMMARY.md` documenting: the 4 tool keys + their requiresApproval values, the 4 new pnpm scripts, the D-01 in-runner-vs-hardgate distinction, and confirmation that custom-tools.ts compiles against the new packages.
</output>
