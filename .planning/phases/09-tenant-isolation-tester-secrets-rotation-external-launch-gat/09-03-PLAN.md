---
phase: 09-tenant-isolation-tester-secrets-rotation-external-launch-gat
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - packages/core/src/security/vault-rotate.ts
  - packages/core/src/security/vault-rotate.test.ts
  - packages/core/src/security/access-audit.ts
  - packages/core/src/security/access-audit.test.ts
  - packages/core/src/security/anomaly.ts
  - packages/core/src/security/anomaly.test.ts
  - packages/core/src/security/refreshers/close.ts
  - packages/core/src/security/refreshers/stubs.ts
  - packages/core/src/index.ts
autonomous: true
requirements: [SC-9-2]
must_haves:
  truths:
    - "rotateCredential(db, key, mcpId, refresher) decrypts existing token, calls refresher, re-stores; records 'rotation' finding on failure"
    - "findOrphanedGrants(db, tenantId) returns OAuth credentials whose bound agent is archived >30d (Pitfall 4 mitigation — does NOT flag every archived binding)"
    - "detectUsageSpikes(db, tenantId) runs windowed scan over audit_log (24h vs 7d baseline) with absolute floor of >10 events (Pitfall 3 mitigation)"
    - "Close OAuth refresher is a reference implementation; Meta + Stripe refreshers are stubs that throw 'operator: implement provider refresher' (D-03 — fail closed, not silently)"
  artifacts:
    - path: packages/core/src/security/vault-rotate.ts
      provides: "rotateCredential helper; reuses vault decrypt/storeCredential"
      exports: ["rotateCredential", "RotateResult"]
    - path: packages/core/src/security/access-audit.ts
      provides: "findOrphanedGrants tenant-scoped query"
      exports: ["findOrphanedGrants"]
    - path: packages/core/src/security/anomaly.ts
      provides: "detectUsageSpikes 24h-rolling scan"
      exports: ["detectUsageSpikes"]
    - path: packages/core/src/security/refreshers/close.ts
      provides: "Close OAuth refresher reference impl"
      exports: ["closeRefresher"]
    - path: packages/core/src/security/refreshers/stubs.ts
      provides: "Meta + Stripe stub refreshers (throw on call)"
      exports: ["metaRefresher", "stripeRefresher"]
  key_links:
    - from: packages/core/src/security/vault-rotate.ts
      to: packages/vault
      via: import { resolveAccessToken, storeCredential, decrypt, Refresher } from "@agent-os/vault"
      pattern: "from \"@agent-os/vault\""
    - from: packages/core/src/security/vault-rotate.ts
      to: packages/core/src/security/findings.ts
      via: import { recordFinding } from "./findings.js"
      pattern: "recordFinding"
    - from: packages/core/src/security/access-audit.ts
      to: agents.lifecycle_state
      via: SQL filter `a.lifecycle_state = 'archived' and a.lifecycle_changed_at < now() - interval '30 days'`
      pattern: "lifecycle_state = 'archived'"
---

<objective>
Ship the three lightweight deterministic tools — vault-rotate, access-audit, anomaly — as modules inside `@agent-os/core` per packaging recommendation A1. Each is a single SQL aggregation or vault-call wrapper. All three are tested standalone (tsx, no vitest) per S5. All three write findings via `recordFinding` from 09-01.

Purpose: these are the bodies the `secrets-rotation`, `access-auditor`, and `security-anomaly-watchdog` agents call via runner `customToolDispatch`. They're small (~50-100 LOC each), share infrastructure (Db, findings, vault), and don't earn their own packages.

Output: 3 modules + 3 tests + 2 refresher files (Close ref impl + Meta/Stripe stubs) + re-exports from core/src/index.ts.
</objective>

<execution_context>
@/home/user/agent-os/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/agent-os/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/user/agent-os/.planning/phases/09-tenant-isolation-tester-secrets-rotation-external-launch-gat/09-RESEARCH.md
@/home/user/agent-os/.planning/phases/09-tenant-isolation-tester-secrets-rotation-external-launch-gat/09-PATTERNS.md
@/home/user/agent-os/.planning/phases/09-tenant-isolation-tester-secrets-rotation-external-launch-gat/09-CONTEXT.md
@/home/user/agent-os/.planning/phases/09-tenant-isolation-tester-secrets-rotation-external-launch-gat/09-01-SUMMARY.md

<interfaces>
<!-- Existing shapes the executor depends on -->

From packages/vault/src/index.ts (lines 43-105 — the substrate this plan composes):
- `type Refresher = (refreshToken: string) => Promise<{ accessToken: string; refreshToken?: string; expiresAt?: Date | null } | null>`
- `resolveAccessToken(db, key, mcpId, refresher?): Promise<string | null>` — the lazy-refresh path
- `storeCredential(db, key, { tenantId, mcpId, accessToken, refreshToken, scopes, expiresAt }): Promise<void>` — the writer
- `decrypt(vaultRef: string, key: Buffer): string` — token decryption
- `makeBundleTokenResolver(...)` — short-TTL bundle tokens (≤300s) — don't invalidate in-flight; let them expire

From packages/db/src/schema.ts:
- `oauthCredentials` (line 241): { id, tenantId, mcpId, vaultRef, scopes, expiresAt, ... }
- `agents` (line 91): { id, tenantId, key, lifecycleState ('active' | 'archived' | ...), lifecycleChangedAt, ... }
- `agentMcps`, `mcps`, `auditLog`, `autonomyEvents` — referenced in join queries

From packages/core/src/security/findings.ts (from plan 09-01):
- `recordFinding(db, { tenantId, agentId?, category, severity, title, payload? })` → row

From packages/core/src/lifecycle.ts:
- The pattern for drizzle `sql` tagged-template aggregations + tenant-scoped reads (the analog for access-audit + anomaly per PATTERNS.md)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build vault-rotate.ts + refreshers (Close ref + Meta/Stripe stubs)</name>
  <files>packages/core/src/security/vault-rotate.ts, packages/core/src/security/vault-rotate.test.ts, packages/core/src/security/refreshers/close.ts, packages/core/src/security/refreshers/stubs.ts</files>
  <read_first>
    - packages/vault/src/index.ts lines 43-105 (Refresher type + resolveAccessToken + storeCredential + decrypt — the exact substrate)
    - packages/core/src/security/findings.ts (from 09-01 — recordFinding signature)
    - packages/tool-browser/src/tool-browser.test.ts (S5 tsx test harness)
    - .planning/phases/09-tenant-isolation-tester-secrets-rotation-external-launch-gat/09-PATTERNS.md lines 463-505 (vault-rotate.ts skeleton including D-03 stub pattern)
    - .planning/phases/09-tenant-isolation-tester-secrets-rotation-external-launch-gat/09-RESEARCH.md Pitfall 2 (race condition) and D-03 (scope: Close = reference, Meta/Stripe = stub)
  </read_first>
  <behavior>
    - `rotateCredential(db, key, mcpId, refresher)`: (1) select oauth_credentials by mcpId; (2) if no expiresAt → return `{ rotated: false, reason: "no expiry — manual provider rotation required" }`; (3) decrypt vaultRef; (4) if no refresh token → return `{ rotated: false, reason: "no refresh token — needs_reauth flow" }`; (5) call refresher; (6) if refresher returns null → record 'rotation' high-severity finding + return `{ rotated: false, reason: "provider refresh failed" }`; (7) storeCredential with new tokens; (8) return `{ rotated: true }`.
    - `closeRefresher: Refresher` calls Close.io OAuth refresh endpoint (per D-03 reference implementation); reads CLOSE_OAUTH_CLIENT_ID + CLOSE_OAUTH_CLIENT_SECRET from env; returns { accessToken, refreshToken, expiresAt } or null on HTTP failure.
    - `metaRefresher` + `stripeRefresher`: each is `async () => { throw new Error("operator: implement provider refresher — see docs/HANDOFF-other-session.md"); }`. Fail closed (D-03) — never silently succeed without a real refresh.
    - tsx test covers: (a) `rotateCredential` is async function; (b) given a mocked db returning a cred with no expiresAt, returns `rotated: false`; (c) Meta/Stripe stubs throw the expected error; (d) closeRefresher exists as a function. NO live HTTP calls in the test (mock fetch via global override or skip the HTTP path).
  </behavior>
  <action>
    Implement `vault-rotate.ts` per PATTERNS.md lines 463-498 skeleton. Use `recordFinding` from 09-01 for the failure path; `recordFinding(db, { tenantId: cred.tenantId, category: "rotation", severity: "high", title: \`rotation failed for mcp \${mcpId}\`, payload: { mcpId } })`.

    For Pitfall 2 mitigation (race condition mid-agent-run): scope rotation to scheduled windows in the doctrine — the `secrets-rotation` agent runs cron 04:00 daily, in-flight bundle tokens (≤300s TTL per `makeBundleTokenResolver`) expire naturally rather than being invalidated. Add a comment block at the top of `vault-rotate.ts` citing Pitfall 2 and the 300s TTL contract; the agent's cron schedule is set in its seed (plan 09-04).

    Implement `refreshers/close.ts` per D-03 reference impl — Close.io documented OAuth refresh URL (https://api.close.com/oauth2/token), POST `grant_type=refresh_token` + refresh_token + client_id + client_secret; parse JSON response → return `{ accessToken, refreshToken, expiresAt: new Date(Date.now() + expires_in * 1000) }`; on non-200, return null.

    Implement `refreshers/stubs.ts` with two named exports `metaRefresher` and `stripeRefresher` that throw the exact error string per D-03.

    Re-export from `packages/core/src/index.ts`: `rotateCredential`, `RotateResult`, `closeRefresher`, `metaRefresher`, `stripeRefresher`. (Findings already re-exported by 09-01.)

    Mock-friendly test: implement a tiny `mockDb` object literal that satisfies the minimal Drizzle surface the function uses (one `.select().from().where()` chain — see how tool-browser.test handles its own mocking, or use a thin Proxy that records calls). Do NOT attempt a real DB connection.
  </action>
  <verify>
    <automated>pnpm --filter @agent-os/core typecheck</automated>
    <automated>pnpm --filter @agent-os/core test:security</automated>
    <automated>node -e "const c = require('fs').readFileSync('packages/core/src/security/refreshers/stubs.ts','utf8'); if (!/operator: implement provider refresher/.test(c)) { console.error('stub error string missing'); process.exit(1); } if ((c.match(/throw new Error/g)||[]).length < 2) { console.error('stubs do not throw'); process.exit(1); } console.log('OK')"</automated>
    <automated>grep -c 'recordFinding' packages/core/src/security/vault-rotate.ts</automated>
  </verify>
  <done>vault-rotate.ts implements the rotate flow with finding-on-failure; Close refresher exists as reference; Meta + Stripe stubs throw expected error; tests pass; core typechecks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build access-audit.ts + anomaly.ts with tests, re-export from core</name>
  <files>packages/core/src/security/access-audit.ts, packages/core/src/security/access-audit.test.ts, packages/core/src/security/anomaly.ts, packages/core/src/security/anomaly.test.ts, packages/core/src/index.ts</files>
  <read_first>
    - packages/core/src/lifecycle.ts (drizzle `sql` aggregation pattern + tenant-scoped read example)
    - packages/db/src/schema.ts lines 91-130 (agents.lifecycleState + lifecycleChangedAt — Phase 8.5 non-destructive archive)
    - packages/db/src/schema.ts lines 241-260 (oauthCredentials shape)
    - packages/db/src/schema.ts lines 340-360 (auditLog + autonomyEvents — the time-windowed source data)
    - .planning/phases/09-tenant-isolation-tester-secrets-rotation-external-launch-gat/09-PATTERNS.md lines 510-565 (access-audit + anomaly skeletons)
    - .planning/phases/09-tenant-isolation-tester-secrets-rotation-external-launch-gat/09-RESEARCH.md Pitfall 3 (audit log volume → window scoped) and Pitfall 4 (archived-agent false-positive policy)
  </read_first>
  <behavior>
    - `findOrphanedGrants(db, tenantId)`: SQL joins oauth_credentials → agent_mcps → agents → mcps; filter `a.lifecycle_state = 'archived' AND a.lifecycle_changed_at < now() - interval '30 days'`. Returns rows for review. Pitfall 4: does NOT flag every archived-agent binding (Phase 8.5 keeps history); only archived >30d.
    - `detectUsageSpikes(db, tenantId, window?)`: SQL CTE with current_window (24h) + baseline (8d ago → 1d ago, daily_avg = count/7); selects tool_name where current count > 10 (absolute floor) AND (no baseline OR ratio > 5×). Window param defaults to 24h. Returns rows ready for `recordFinding` writeback.
    - `access-audit.test.ts`: asserts function is async + exported; (optional) snapshot the SQL string to lock the filter clauses (`lifecycle_state = 'archived'` and `interval '30 days'`).
    - `anomaly.test.ts`: asserts function is async + exported; snapshot/grep the SQL for the 24h window literal + the >10 absolute floor + the 5× ratio (Pitfall 3 mitigation locked in).
    - `packages/core/src/index.ts` re-exports: `findOrphanedGrants`, `detectUsageSpikes`.
  </behavior>
  <action>
    Implement `access-audit.ts` per PATTERNS.md lines 514-532 skeleton. Tenant-scoped via `where oc.tenant_id = ${tenantId}` parameter (drizzle parameterization — no string interpolation).

    Implement `anomaly.ts` per PATTERNS.md lines 542-565 skeleton. The CTE structure (current_window + baseline + ratio) is the Pitfall 3 mitigation — encode the 24h window + 7d baseline + absolute floor + ratio threshold as inline constants, not knobs. (Day-1 thresholds are intentionally loose per D-07; tightening goes through agent-evaluator scorecard later, not via param surgery.)

    Re-export from `packages/core/src/index.ts`. Place the security exports next to the existing security/architect re-exports.

    Tests use a SQL-string snapshot pattern: capture the rendered SQL via a mock `db.execute` that records the input and assert grep-style on the recorded SQL text (e.g. `expect(captured).toMatch(/interval '30 days'/)`). This locks the filter clauses against regression without needing a live DB.
  </action>
  <verify>
    <automated>pnpm --filter @agent-os/core typecheck</automated>
    <automated>pnpm --filter @agent-os/core test:security</automated>
    <automated>grep -c "lifecycle_state = 'archived'\|interval '30 days'" packages/core/src/security/access-audit.ts</automated>
    <automated>grep -c "interval\|nullif\|ratio" packages/core/src/security/anomaly.ts</automated>
    <automated>node -e "const c = require('fs').readFileSync('packages/core/src/index.ts','utf8'); for (const ex of ['findOrphanedGrants','detectUsageSpikes','rotateCredential']) { if (!c.includes(ex)) { console.error('missing re-export:', ex); process.exit(1); } } console.log('OK')"</automated>
  </verify>
  <done>Both modules + tests compile and pass; SQL grep gates green (Pitfall 3 + Pitfall 4 mitigations encoded in source); all 3 security exports surface from core.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application → vault decrypt | rotateCredential reads AOS_VAULT_KEY; consumed only inside the helper, never logged or returned |
| application → external OAuth provider | closeRefresher posts to Close.io with client secret; HTTPS-only |
| operator → audit_log queries | anomaly.ts reads but never writes audit_log (Repudiation guarantee preserved) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-02 | Tampering / DoS | vault rotation race | mitigate | (a) Cron-windowed rotation (04:00 daily — set in 09-04 seed); (b) in-flight bundle tokens ≤300s TTL (existing vault contract — don't invalidate, let expire); (c) findings on failure surface stale-rotation events early. Per D-03 + Pitfall 2. |
| T-9-03 | DoS | audit_log volume crushes anomaly scan | mitigate | (a) 24h rolling window only (never full history); (b) absolute floor `n > 10` short-circuits trivial counts; (c) `audit_log_tenant_ts_idx` from migration 0010 (added in 09-01). |
| T-9-04 | Repudiation / FP | false-positive flood from anomaly + access-audit | mitigate | D-07 loose thresholds documented; access-audit filters to `archived > 30 days` (Pitfall 4 mitigation); agent-evaluator tightens over time, not this phase. |
| T-9-12 | Spoofing | refresher silently succeeds without rotating | mitigate | D-03 stubs THROW; never return null/undefined for unimplemented providers. Fail closed. |
| T-9-13 | Information Disclosure | recordFinding payload leaks raw OAuth token | mitigate | vault-rotate payload contains only `{ mcpId }` — token never crosses into findings. V8 ASVS satisfied. |
| T-9-SC | Tampering | npm install | mitigate | No new external packages — refreshers use Node 22 native `fetch` (no node-fetch dep). All existing deps audited in prior phases. |
</threat_model>

<verification>
- `pnpm --filter @agent-os/core typecheck` green
- `pnpm --filter @agent-os/core test:security` green (all 3 new security test files pass)
- access-audit.ts grep: `lifecycle_state = 'archived'` AND `interval '30 days'` both present
- anomaly.ts grep: `interval` + `nullif` + `ratio` all present (Pitfall 3 mitigation encoded)
- Meta + Stripe refreshers throw the exact `operator: implement provider refresher` string
- All 3 functions re-exported from `packages/core/src/index.ts`
</verification>

<success_criteria>
- SC-9-2 advanced: 3 of 3 ops tools (vault-rotate, access-audit, anomaly) have their bodies. Registry seeds + runner dispatch land in 09-04.
- D-03 honored: Close = reference, Meta + Stripe = throw stubs.
- D-07 honored: anomaly thresholds intentionally loose; access-audit filters archived-30d only.
</success_criteria>

<output>
Create `.planning/phases/09-tenant-isolation-tester-secrets-rotation-external-launch-gat/09-03-SUMMARY.md` documenting: the 3 module signatures, the refresher provider matrix (Close=ref / Meta=stub / Stripe=stub), the audit_log volume strategy (24h window + absolute floor + 5× ratio), and the archived-30d filter for orphan checks.
</output>
