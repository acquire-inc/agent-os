---
phase: 09-tenant-isolation-tester-secrets-rotation-external-launch-gat
plan: 05
type: execute
wave: 3
depends_on: ["09-01", "09-04"]
files_modified:
  - scripts/seed/acqu-tenant-isolation-tester.ts
  - scripts/seed/acqu-secrets-rotation.ts
  - scripts/seed/acqu-access-auditor.ts
  - scripts/seed/acqu-security-anomaly-watchdog.ts
  - external/acqu-skills/tenant-isolation-testing/SKILL.md
  - external/acqu-skills/secrets-rotation/SKILL.md
  - external/acqu-skills/access-audit/SKILL.md
  - external/acqu-skills/anomaly-detection-security/SKILL.md
  - packages/core/src/architect/architect.test.ts
  - scripts/seed/package.json
autonomous: true
requirements: [SC-9-1, SC-9-5]
must_haves:
  truths:
    - "4 T-critical agent specs exist with script literal `anthropic/claude-opus-4.8` (NEVER Hermes in script per CLAUDE.md)"
    - "Each agent's tool binding matches the tool key seeded in 09-04 (tenant-isolation-tester→tool.rls-test; secrets-rotation→tool.vault-rotate; access-auditor→tool.access-audit; security-anomaly-watchdog→tool.access-log-analyzer)"
    - "Each agent has cron schedule, autonomy, budgetCapUsd, escalationPolicy per doctrine v2 D5.3"
    - "4 skill stubs exist under external/acqu-skills/{slug}/SKILL.md with doctrine procedure content"
    - "architect.test.ts asserts all 4 Phase-9 keys are in CANT_FAIL_KEYS (regression lock)"
  artifacts:
    - path: scripts/seed/acqu-tenant-isolation-tester.ts
      provides: "agent spec + standalone runner; tool.rls-test binding; cron 30 4 * * *"
      contains: "anthropic/claude-opus-4.8"
    - path: scripts/seed/acqu-secrets-rotation.ts
      provides: "agent spec; tool.vault-rotate binding; cron 0 4 * * *"
      contains: "anthropic/claude-opus-4.8"
    - path: scripts/seed/acqu-access-auditor.ts
      provides: "agent spec; tool.access-audit binding; cron 0 5 * * 3"
      contains: "anthropic/claude-opus-4.8"
    - path: scripts/seed/acqu-security-anomaly-watchdog.ts
      provides: "agent spec; tool.access-log-analyzer binding; cron hourly"
      contains: "anthropic/claude-opus-4.8"
    - path: packages/core/src/architect/architect.test.ts
      provides: "4 new assertions locking the can't-fail keys"
      contains: "tenant-isolation-tester"
  key_links:
    - from: scripts/seed/acqu-tenant-isolation-tester.ts
      to: tool.rls-test (registry, seeded in 09-04)
      via: tools array in AgentSpec
      pattern: "tool.rls-test"
    - from: scripts/seed/acqu-secrets-rotation.ts
      to: tool.vault-rotate (requiresApproval=true → propose-gated)
      via: tools array
      pattern: "tool.vault-rotate"
---

<objective>
Seed the 4 doctrine agents as data. Each is a near-verbatim copy of `acqu-ad-claim-compliance.ts` (the T-critical template per PATTERNS.md), with only key/name/SYSTEM_PROMPT/cron/tools/budgetCapUsd/escalationPolicy varying. Add 4 skill stubs (doctrine workflow content) and extend architect.test.ts to lock the can't-fail regression. Per CLAUDE.md non-negotiable #1: agents are DATA, not modules.

Purpose: with tools registered (09-04), runner dispatch wired (09-04), and security_findings substrate landed (09-01), the agents themselves are the final composition step. SeedAgent (Phase 2/8.5) handles the tenant default_model_override rewrite automatically — script literals stay `claude-opus-4.8` so doctrine intent is preserved when override is cleared.

Output: 4 agent seeds + 4 skill stubs + 1 modified architect test + 4 new pnpm scripts.
</objective>

<execution_context>
@/home/user/agent-os/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/agent-os/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/user/agent-os/.planning/phases/09-tenant-isolation-tester-secrets-rotation-external-launch-gat/09-RESEARCH.md
@/home/user/agent-os/.planning/phases/09-tenant-isolation-tester-secrets-rotation-external-launch-gat/09-PATTERNS.md
@/home/user/agent-os/.planning/phases/09-tenant-isolation-tester-secrets-rotation-external-launch-gat/09-CONTEXT.md
@/home/user/agent-os/.planning/phases/09-tenant-isolation-tester-secrets-rotation-external-launch-gat/09-01-SUMMARY.md
@/home/user/agent-os/.planning/phases/09-tenant-isolation-tester-secrets-rotation-external-launch-gat/09-04-SUMMARY.md
@/home/user/agent-os/docs/acqu-agent-doctrine-v2.md
@/home/user/agent-os/CLAUDE.md

<interfaces>
<!-- Existing shapes the executor depends on -->

From scripts/seed/acqu-ad-claim-compliance.ts (53 lines — read in full as the verbatim template; PATTERNS.md confirms this is the EXACT analog):
- Imports: `TENANT_IDS` from `@agent-os/shared`; `runStandalone` from `./lib/runSpec.js`; `AgentSpec` type from `@agent-os/core`
- 4-line header comment block citing CLAUDE.md can't-fail list
- SYSTEM_PROMPT const (doctrine prompt verbatim)
- `export const <key>Spec: AgentSpec = { tenantId, key, name, systemPrompt, model: "anthropic/claude-opus-4.8", thinkingLevel: "high", autonomy, knowledgeScope, budgetCapUsd, cron, tools, skills, mcpNames, escalationPolicy }`
- Tail: `if (import.meta.url === \`file://${process.argv[1]}\`) void runStandalone(<key>Spec);`

From packages/core/src/seed/seedAgent.ts (Phase 8.5 — tenant override rewrite):
- `seedAgent(db, spec)` checks `tenants.default_model_override`; if set and `spec.model !== override`, REWRITES at insert time + prints warning (lines 372-384 per PATTERNS.md S4)
- Script literals therefore stay doctrine-default; operator override is the runtime knob

From doctrine v2 D5.3 (lines 1206-1362) — the 4 agent specs:
- tenant-isolation-tester (lines 1280-1325): autonomy execute_safe; cron 04:30 daily; budget 1.50; escalation tcritical:isolation_failure→founder_p0
- secrets-rotation (lines 1240-1280): autonomy execute_safe (internal) / propose (client OAuth — enforced via requiresApproval=true on tool, not via spec); cron 04:00 daily; budget 0.50; escalation tcritical:rotation_failed→founder_p0
- access-auditor (lines 1260-1280): autonomy execute_safe; cron Wed 05:00 weekly (0 5 * * 3); budget 1.00; escalation tcritical:orphan_grant→founder_p1
- security-anomaly-watchdog (lines 1340-1362): autonomy execute_safe (D-07 alerts only); cron hourly (0 * * * *); budget 0.30; escalation tcritical:anomaly_high→founder_p0

From scripts/seed/package.json: 1 new script per agent (`acqu-tenant-isolation-tester`, etc.) following existing `tsx <name>.ts` pattern.

From packages/core/src/architect/architect.test.ts lines 153-183: the can't-fail regression block to extend.

From external/acqu-skills/<existing>/SKILL.md (e.g. verification-before-completion): each SKILL.md has H1 `# SKILL: <name>` + `## Purpose` + `## Workflow` + `## Rules` sections — copy this shape.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 4 T-critical agent seed scripts + 4 skill stubs + scripts/seed/package.json entries</name>
  <files>scripts/seed/acqu-tenant-isolation-tester.ts, scripts/seed/acqu-secrets-rotation.ts, scripts/seed/acqu-access-auditor.ts, scripts/seed/acqu-security-anomaly-watchdog.ts, external/acqu-skills/tenant-isolation-testing/SKILL.md, external/acqu-skills/secrets-rotation/SKILL.md, external/acqu-skills/access-audit/SKILL.md, external/acqu-skills/anomaly-detection-security/SKILL.md, scripts/seed/package.json</files>
  <read_first>
    - scripts/seed/acqu-ad-claim-compliance.ts (53 lines — full file; the exact T-critical analog per PATTERNS.md S1)
    - scripts/seed/acqu-contract-drafter.ts (the secondary analog called out for secrets-rotation per PATTERNS.md classification table)
    - docs/acqu-agent-doctrine-v2.md lines 1206-1362 (the 4 agent specifications — SYSTEM_PROMPT content + cron + autonomy + escalation)
    - .planning/phases/09-tenant-isolation-tester-secrets-rotation-external-launch-gat/09-PATTERNS.md lines 47-107 (per-agent deltas table — autonomy, cron, budget, tool key, escalationPolicy per agent)
    - external/acqu-skills/verification-before-completion/SKILL.md (the existing SKILL.md format to mirror — H1 + Purpose + Workflow + Rules)
    - scripts/seed/package.json (the scripts block to extend with 4 new `tsx <name>.ts` entries)
  </read_first>
  <action>
    Copy `scripts/seed/acqu-ad-claim-compliance.ts` 4 times — once per Phase-9 agent. Per file:

    1. Update header comment (CRITICAL — required for audit grep per PATTERNS.md):
    ```
    // scripts/seed/acqu-<key>.ts
    // Source: v2 §D5.3 · main §1.5 + CLAUDE.md can't-fail list.
    // HARD GATE #2 (main §6): no external Cliently launch until tenant-isolation-tester passes.
    // Model MUST be claude-opus-4.8 — NEVER Hermes.
    ```

    2. Paste SYSTEM_PROMPT VERBATIM from the doctrine v2 D5.3 sub-section for that agent (lines 1311-1325 for tenant-isolation-tester; locate analogous blocks for the other 3).

    3. Build AgentSpec per the per-agent deltas table:

    | Agent | autonomy | cron | budget | tools[].key (single tool) | escalationPolicy |
    |---|---|---|---|---|---|
    | tenant-isolation-tester | execute_safe | `30 4 * * *` (jobName `tenant-isolation-tester-daily`) | 1.50 | tool.rls-test | tcritical:isolation_failure -> founder_p0 |
    | secrets-rotation | execute_safe | `0 4 * * *` (jobName `secrets-rotation-daily`) | 0.50 | tool.vault-rotate | tcritical:rotation_failed -> founder_p0 |
    | access-auditor | execute_safe | `0 5 * * 3` (jobName `access-auditor-weekly`) | 1.00 | tool.access-audit | tcritical:orphan_grant -> founder_p1 |
    | security-anomaly-watchdog | execute_safe | `0 * * * *` (jobName `security-anomaly-watchdog-hourly`) | 0.30 | tool.access-log-analyzer | tcritical:anomaly_high -> founder_p0 |

    4. Common fields for ALL 4:
       - `tenantId: TENANT_IDS.acqu`
       - `model: "anthropic/claude-opus-4.8"` with the comment line `// T-CRITICAL — Claude Opus 4.8 per CLAUDE.md can't-fail list. NEVER change to Hermes.`
       - `thinkingLevel: "high"`
       - `knowledgeScope: { folders: ["security"], tags: ["security"] }`
       - `mcpNames: ["Slack"]` (for alert routing)
       - `skills`: array with 2 entries — the agent's primary skill + `verification-before-completion` (per analog pattern). Skill keys mirror the 4 stubs being created in this task.
       - Tail: `if (import.meta.url === \`file://${process.argv[1]}\`) void runStandalone(<key>Spec);`

    Skill stubs — create 4 new files following the existing SKILL.md format. Each file has:
    ```
    # SKILL: <name>

    ## Purpose
    <one-paragraph distillation from v2 D5.3 sub-section>

    ## Workflow
    <bulleted steps from the doctrine for this agent>

    ## Rules
    <the doctrine's non-negotiables for this agent — e.g. for secrets-rotation, the "never rotate client OAuth unilaterally" rule from v2 D5.3 L1254>
    ```

    Map (skill key → seed filename):
    - tenant-isolation-testing → tenant-isolation-tester
    - secrets-rotation → secrets-rotation
    - access-audit → access-auditor
    - anomaly-detection-security → security-anomaly-watchdog

    Append 4 entries to `scripts/seed/package.json` scripts:
    ```
    "acqu-tenant-isolation-tester": "tsx acqu-tenant-isolation-tester.ts",
    "acqu-secrets-rotation": "tsx acqu-secrets-rotation.ts",
    "acqu-access-auditor": "tsx acqu-access-auditor.ts",
    "acqu-security-anomaly-watchdog": "tsx acqu-security-anomaly-watchdog.ts",
    ```

    For secrets-rotation specifically: the autonomy stays `execute_safe` because the propose-gating for client OAuth is enforced at the TOOL level (tool.vault-rotate has requiresApproval=true from 09-04). The agent-level autonomy = `execute_safe` permits the agent to call the tool; PreToolUse hook then pauses for approval on the actual rotation. This separation is intentional — cite v2 D5.3 L1254 in a comment near the autonomy field.
  </action>
  <verify>
    <automated>pnpm --filter @agent-os/seed typecheck</automated>
    <automated>for f in scripts/seed/acqu-tenant-isolation-tester.ts scripts/seed/acqu-secrets-rotation.ts scripts/seed/acqu-access-auditor.ts scripts/seed/acqu-security-anomaly-watchdog.ts; do grep -q 'anthropic/claude-opus-4.8' "$f" || { echo "FAIL: $f missing opus literal"; exit 1; }; grep -q 'T-CRITICAL\|NEVER change to Hermes\|can.t-fail list' "$f" || { echo "FAIL: $f missing T-critical comment"; exit 1; }; done; echo OK</automated>
    <automated>for s in tenant-isolation-testing secrets-rotation access-audit anomaly-detection-security; do test -f "external/acqu-skills/$s/SKILL.md" && grep -q '^# SKILL:' "external/acqu-skills/$s/SKILL.md" || { echo "FAIL: skill $s missing or malformed"; exit 1; }; done; echo OK</automated>
    <automated>node -e "const p = JSON.parse(require('fs').readFileSync('scripts/seed/package.json','utf8')); for (const k of ['acqu-tenant-isolation-tester','acqu-secrets-rotation','acqu-access-auditor','acqu-security-anomaly-watchdog']) { if (!p.scripts[k]) { console.error('missing script:', k); process.exit(1); } } console.log('OK')"</automated>
    <automated>node -e "const c = require('fs').readFileSync('scripts/seed/acqu-tenant-isolation-tester.ts','utf8'); if (!/tool\.rls-test/.test(c)) { console.error('tool binding missing'); process.exit(1); } if (!/30 4 \* \* \*/.test(c)) { console.error('cron schedule wrong'); process.exit(1); } console.log('OK')"</automated>
  </verify>
  <done>All 4 agent seed scripts have opus literal + T-critical comment + correct tool binding + correct cron; 4 skill stubs exist with H1 SKILL:; 4 pnpm scripts wired; typecheck green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend architect.test.ts to lock all 4 Phase-9 can't-fail regressions</name>
  <files>packages/core/src/architect/architect.test.ts</files>
  <read_first>
    - packages/core/src/architect/architect.test.ts lines 153-183 (the existing can't-fail block — covers ad-claim-compliance; pattern to extend)
    - packages/core/src/architect/hydrate.ts lines 21-35 (the CANT_FAIL_KEYS set — confirms all 4 keys are now present after 09-01 added `secrets-rotation`)
    - .planning/phases/09-tenant-isolation-tester-secrets-rotation-external-launch-gat/09-PATTERNS.md lines 638-651 (the assertion extension pattern)
  </read_first>
  <behavior>
    - 4 new assertions in architect.test.ts immediately after the existing can't-fail block:
      ```
      assert(isCantFail("tenant-isolation-tester"), "isCantFail recognizes tenant-isolation-tester");
      assert(isCantFail("secrets-rotation"), "isCantFail recognizes secrets-rotation");
      assert(isCantFail("access-auditor"), "isCantFail recognizes access-auditor");
      assert(isCantFail("security-anomaly-watchdog"), "isCantFail recognizes security-anomaly-watchdog");
      ```
    - Architect test count climbs from 33 → 37 (or whatever current count + 4).
    - All 4 assertions PASS (because 09-01 added `secrets-rotation` and the other 3 were already present per the hydrate.ts review).
    - D-08 verification: `pnpm --filter @agent-os/core run test:architect` reports new total, all green.
  </behavior>
  <action>
    Open `packages/core/src/architect/architect.test.ts`. Find the existing can't-fail block (lines 153-183 per PATTERNS.md). After the last existing assertion in that block, add the 4 new lines per the skeleton (use the same `assert` helper already in scope).

    Add a 1-line comment above the new block: `// Phase 9 — can't-fail regression locks for the 4 security agents.`

    Do NOT modify any other test. Do NOT modify hydrate.ts (09-01 already added `secrets-rotation`; this task only LOCKS the regression via test).

    If the test fails because `secrets-rotation` is missing from CANT_FAIL_KEYS, that's a SIGNAL plan 09-01 didn't land its hydrate.ts edit — escalate, don't paper over by changing the test.
  </action>
  <verify>
    <automated>pnpm --filter @agent-os/core run test:architect 2>&1 | tee /tmp/architect-test.out | tail -10</automated>
    <automated>node -e "const o = require('fs').readFileSync('/tmp/architect-test.out','utf8'); const m = o.match(/(\d+)\s+passed/); if (!m || parseInt(m[1]) < 37) { console.error('expected >= 37 architect tests passing, got', m && m[1]); process.exit(1); } console.log('OK:', m[1], 'passed')"</automated>
    <automated>grep -c 'isCantFail("tenant-isolation-tester"\|isCantFail("secrets-rotation"\|isCantFail("access-auditor"\|isCantFail("security-anomaly-watchdog"' packages/core/src/architect/architect.test.ts</automated>
  </verify>
  <done>architect test count is ≥ 37 with all green; 4 new isCantFail assertions present in source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed script → agents table | T-critical agents can ONLY be created via seed scripts (architect refuses to assemble them); CANT_FAIL_KEYS regression locks this property |
| operator default_model_override → seedAgent rewrite | Override is a known knob (D-04 documented); script literal preserves doctrine intent for revert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-05 | Tampering | script literal flipped to Hermes by mistake | mitigate | Two-line defense: (a) 4 grep gates on `anthropic/claude-opus-4.8` in this plan's verify; (b) `seed-phase-9.ts` hard-fail guard in plan 09-06 throws BEFORE DB writes if any T-critical spec's model literal is not opus-4.8. Per PATTERNS.md S1. |
| T-9-09 | Repudiation | CANT_FAIL_KEYS shrinks silently | mitigate | architect.test.ts new assertions FAIL fast if any of the 4 keys are removed from the Set. PR-time signal. |
| T-9-14 | Elevation of Privilege | secrets-rotation agent rotates client OAuth without approval | mitigate | Agent autonomy = execute_safe but tool.vault-rotate has requiresApproval=true (09-04). PreToolUse hook (Phase 1, 1c) pauses for human approval on every rotation invocation. v2 D5.3 L1254 satisfied. |
| T-9-16 | Information Disclosure | skill markdown leaks internal procedures externally | accept | Skills are operator-facing reference docs; doctrine content is already in public /docs/ tree. No new exposure. |
| T-9-SC | Tampering | npm install | mitigate | No new external packages added — seed scripts import only existing workspace packages. |
</threat_model>

<verification>
- `pnpm --filter @agent-os/seed typecheck` green
- `pnpm --filter @agent-os/core run test:architect` green with count ≥ 37 (33 baseline + 4 new)
- All 4 agent seed files contain `anthropic/claude-opus-4.8` literal + T-critical comment
- All 4 agent seed files reference the correct tool key (tool.rls-test / tool.vault-rotate / tool.access-audit / tool.access-log-analyzer)
- All 4 skill SKILL.md files exist with H1 `# SKILL:` header
- 4 new pnpm `seed:acqu-*` scripts wired
- Cron schedules per doctrine: 30 4 * * * / 0 4 * * * / 0 5 * * 3 / 0 * * * *
</verification>

<success_criteria>
- SC-9-1 partially advanced: 4 agent seed scripts exist with script-literal opus-4.8. Batch runner + actual seeding land in 09-06.
- SC-9-5 complete: architect 37/37 includes the 4 Phase-9 regression locks.
</success_criteria>

<output>
Create `.planning/phases/09-tenant-isolation-tester-secrets-rotation-external-launch-gat/09-05-SUMMARY.md` documenting: the 4 agent keys + their cron + autonomy + budget + tool binding + escalation, the 4 new skill slugs, the architect test count delta (33→37+), and the script-literal doctrine that operator override transparently rewrites at seed time.
</output>
