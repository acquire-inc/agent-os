---
phase: 09-tenant-isolation-tester-secrets-rotation-external-launch-gat
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/tool-rls-test/package.json
  - packages/tool-rls-test/tsconfig.json
  - packages/tool-rls-test/src/index.ts
  - packages/tool-rls-test/src/types.ts
  - packages/tool-rls-test/src/impersonate.ts
  - packages/tool-rls-test/src/attack-vectors.ts
  - packages/tool-rls-test/src/tool-rls-test.test.ts
  - packages/tool-rls-test/README.md
autonomous: true
requirements: [SC-9-2, SC-9-4]
must_haves:
  truths:
    - "@agent-os/tool-rls-test is a new workspace package mirroring @agent-os/tool-browser shape"
    - "runIsolationSuite(input) validates input with zod, runs every frozen ATTACK_VECTOR, writes results to a tmpfile, returns { resultsPath, passed, count }"
    - "asUser(db, userId, fn) sets `request.jwt.claim.sub` GUC in a transaction (D-01 — connects via authenticated-role db, NOT service-role)"
    - "ATTACK_VECTORS is Object.freeze'd; assertVectorsAppendOnly throws on shrink (regression doctrine)"
    - "Every vector pairs a positive-control read (returns ≥1 as tenant B) with a negative-control read (returns 0 as tenant A) — Pitfall 5"
  artifacts:
    - path: packages/tool-rls-test/package.json
      provides: "workspace package manifest @agent-os/tool-rls-test"
      contains: "@agent-os/tool-rls-test"
    - path: packages/tool-rls-test/src/index.ts
      provides: "runIsolationSuite + re-exports"
      exports: ["runIsolationSuite", "IsolationInput", "IsolationResult", "asUser", "ATTACK_VECTORS"]
    - path: packages/tool-rls-test/src/attack-vectors.ts
      provides: "Append-only frozen registry covering every tenant-scoped table"
      contains: "Object.freeze"
    - path: packages/tool-rls-test/src/impersonate.ts
      provides: "asUser GUC impersonation helper"
      contains: "set_config('request.jwt.claim.sub'"
  key_links:
    - from: packages/tool-rls-test/src/index.ts
      to: ATTACK_VECTORS
      via: for-of loop awaiting each .run(db, parsed)
      pattern: "for .*ATTACK_VECTORS"
    - from: packages/tool-rls-test/src/attack-vectors.ts
      to: asUser
      via: import from ./impersonate.js
      pattern: "from \"./impersonate"
---

<objective>
Ship the hard-gate engine: a standalone workspace package `@agent-os/tool-rls-test` that runs cross-tenant query attacks against every RLS-protected table, with positive controls to prove the test is wired (Pitfall 5). This is the engine; the registry seed, runner dispatch, and live verification all depend on this package.

Purpose: per D-02 own-package decision — RLS test logic has its own dev surface (impersonation via GUC, append-only vector registry, separate `RLS_TEST_DATABASE_URL` connection) that doesn't belong inside `@agent-os/core`. Mirrors `packages/tool-browser` shape exactly per PATTERNS.md (10 files mapped to that package's analog).

Output: 7 new files in `packages/tool-rls-test/`. `pnpm-workspace.yaml` already includes `packages/*` (verified — Phases 1-8 added every existing package under that glob); no workspace-yaml edit is needed.
</objective>

<execution_context>
@/home/user/agent-os/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/agent-os/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/user/agent-os/.planning/phases/09-tenant-isolation-tester-secrets-rotation-external-launch-gat/09-RESEARCH.md
@/home/user/agent-os/.planning/phases/09-tenant-isolation-tester-secrets-rotation-external-launch-gat/09-PATTERNS.md
@/home/user/agent-os/.planning/phases/09-tenant-isolation-tester-secrets-rotation-external-launch-gat/09-CONTEXT.md

<interfaces>
<!-- Existing shapes the executor depends on -->

From packages/tool-browser/package.json (the verbatim manifest template per PATTERNS.md S5):
- name @agent-os/tool-browser, type module, scripts { typecheck, test }, deps drizzle-orm + zod + workspace pkgs, devDeps @types/node + tsx + typescript

From packages/tool-browser/src/index.ts (the validate→guard→run→return shape):
- import zod schema → parse → call helper → write large result to tmpdir → return { path, passed, count }

From packages/tool-browser/src/types.ts:
- `export const InputSchema = z.object({...}); export type Input = z.infer<typeof InputSchema>; export interface Result {...}`

From packages/tool-browser/src/tool-browser.test.ts (the tsx test harness — pattern S5):
- counters + assert(cond, msg) + main() + process.exit(failed > 0 ? 1 : 0); no vitest

From supabase/migrations/0001_init.sql lines 36-40:
- `auth.uid()` reads `current_setting('request.jwt.claim.sub', true)::uuid` — the EXACT GUC name to set in asUser; do not rename

From supabase/migrations/0001_init.sql + 0006/0007/0008/0009 + 0010 (this phase):
- Tenant-scoped tables (RLS-protected via is_tenant_member): tenants, tenant_members, users, mcps, oauth_credentials, agents, agent_mcps, agent_tools, agent_skills, agent_triggers, runs, run_steps, audit_log, autonomy_events, knowledge_documents, knowledge_chunks, architect_blueprints, tools, approvals, activity_log, security_findings (Phase 9)
- Each table needs one ATTACK_VECTOR row in the registry

From packages/db/src/index.ts:
- `createDb(url: string)` returns a typed drizzle instance; `Db` is the exported type alias

From packages/shared/src/index.ts:
- TENANT_IDS.acqu, TENANT_IDS.cliently — pre-seeded tenant UUIDs for impersonation pairs

From pnpm-workspace.yaml (current state — verified):
- `packages:` list already contains `"packages/*"` (added in Phase 1; confirmed via direct read of the file). The new `packages/tool-rls-test/` directory is automatically picked up — NO edit to pnpm-workspace.yaml is required.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold @agent-os/tool-rls-test package (manifest, tsconfig, types, impersonate)</name>
  <files>packages/tool-rls-test/package.json, packages/tool-rls-test/tsconfig.json, packages/tool-rls-test/src/types.ts, packages/tool-rls-test/src/impersonate.ts, packages/tool-rls-test/README.md</files>
  <read_first>
    - packages/tool-browser/package.json (21 lines — copy verbatim, rename to @agent-os/tool-rls-test, add @agent-os/db workspace dep)
    - packages/tool-browser/tsconfig.json (9 lines — copy verbatim, no edits)
    - packages/tool-browser/src/types.ts (24 lines — the zod input/output template)
    - packages/vault/src/index.ts lines 54-87 (drizzle txn pattern — the analog for asUser)
    - supabase/migrations/0001_init.sql lines 36-40 (auth.uid impl — copy the GUC name verbatim)
    - .planning/phases/09-tenant-isolation-tester-secrets-rotation-external-launch-gat/09-PATTERNS.md lines 266-381 (package.json, tsconfig, types, impersonate skeletons)
  </read_first>
  <behavior>
    - `packages/tool-rls-test/package.json` declares `@agent-os/tool-rls-test`, `private: true`, `type: module`, exports `./src/index.ts`, scripts `typecheck` + `test` (`tsx src/tool-rls-test.test.ts`), deps include `@agent-os/db: workspace:*`, `drizzle-orm`, `zod`, devDeps `@types/node`, `tsx`, `typescript` — all at versions already present in `packages/tool-browser/package.json`.
    - `tsconfig.json` is a verbatim copy of `packages/tool-browser/tsconfig.json`.
    - `src/types.ts`: zod schema `IsolationInputSchema = z.object({ tenantPairs: z.array(z.object({ userA: z.string().uuid(), userB: z.string().uuid(), tenantA: z.string().uuid(), tenantB: z.string().uuid() })).min(1), tables: z.array(z.string()).optional() })`; `IsolationInput` type alias; `IsolationResult` interface `{ resultsPath: string; passed: boolean; count: number }`.
    - `src/impersonate.ts`: `asUser<T>(db, userId, fn)` runs `fn` inside `db.transaction(tx => { await tx.execute(sql\`select set_config('request.jwt.claim.sub', \${userId}, true)\`); return fn(); })`. GUC name MUST be `request.jwt.claim.sub` verbatim (per 0001_init.sql L39 — DO NOT rename).
    - `README.md` documents D-01 explicitly: `RLS_TEST_DATABASE_URL` env var, non-service-role connection requirement, and the "if you see 100% pass in milliseconds and positive controls returned 0, your connection is service-role and the test is lying to you" warning (Pitfall 5).
  </behavior>
  <action>
    Scaffold the package by copying the analog files verbatim (package.json, tsconfig.json from `packages/tool-browser/`, then editing the name + adding `@agent-os/db` workspace dep). Implement `src/types.ts` and `src/impersonate.ts` per the skeletons in PATTERNS.md lines 339-381. Write a focused README that names D-01 directly (Pitfall 1 + Pitfall 5 — separate `RLS_TEST_DATABASE_URL`; positive control is the wiring proof).

    NOTE on `pnpm-workspace.yaml`: `packages/*` is already present in the workspace globs (verified — every existing package under `packages/` is auto-included). No edit required. After `pnpm install`, the new `packages/tool-rls-test` directory will be picked up automatically.

    Pitfall 1 mitigation citation (REQUIRED in README): "This package's helper `asUser` impersonates a tenant via GUC. The Postgres connection used MUST be a non-service-role connection (service-role bypasses RLS by design — a passing test against service-role is meaningless). Set `RLS_TEST_DATABASE_URL` to a Supabase authenticated-role connection string; never re-use `DATABASE_URL` (service-role)."

    Do NOT add a dependency on `postgres` directly — drizzle's `db.execute(sql\`...\`)` is sufficient; the connection is supplied by callers (Wave 3 verification script).
  </action>
  <verify>
    <automated>pnpm install --filter @agent-os/tool-rls-test 2>&1 | tail -5</automated>
    <automated>pnpm --filter @agent-os/tool-rls-test typecheck</automated>
    <automated>node -e "const c = require('fs').readFileSync('packages/tool-rls-test/src/impersonate.ts','utf8'); if (!/set_config\('request\.jwt\.claim\.sub'/.test(c)) { console.error('GUC name wrong or missing'); process.exit(1); } if (!/db\.transaction/.test(c)) { console.error('not txn-wrapped'); process.exit(1); } console.log('OK')"</automated>
    <automated>grep -ci 'RLS_TEST_DATABASE_URL\|service-role' packages/tool-rls-test/README.md</automated>
  </verify>
  <done>Package installs cleanly into workspace (auto-picked up by existing `packages/*` glob); typecheck green; impersonate.ts uses exact GUC name in a transaction; README documents D-01 explicitly (grep hits both terms).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement attack-vectors registry + runIsolationSuite + tsx test</name>
  <files>packages/tool-rls-test/src/attack-vectors.ts, packages/tool-rls-test/src/index.ts, packages/tool-rls-test/src/tool-rls-test.test.ts</files>
  <read_first>
    - packages/tool-browser/src/index.ts (55 lines — the validate→guard→run→return shape per PATTERNS.md lines 302-336)
    - packages/tool-browser/src/tool-browser.test.ts (77 lines — the tsx test harness per PATTERNS.md S5)
    - packages/core/src/architect/hydrate.ts lines 21-35 (the frozen-Set append-only pattern; PATTERNS.md calls this the analog for ATTACK_VECTORS guard)
    - supabase/migrations/0001_init.sql lines 408-462 (the RLS-protected table list — every table in this range needs a vector)
    - supabase/migrations/0006/0007/0008/0009/0010 (additional tenant-scoped tables added in later migrations)
    - .planning/phases/09-tenant-isolation-tester-secrets-rotation-external-launch-gat/09-PATTERNS.md lines 384-422 (attack vector skeleton with positive control) and 427-456 (the test harness skeleton)
  </read_first>
  <behavior>
    - `ATTACK_VECTORS` is `Object.freeze([ ... ])`, one entry per RLS-protected tenant-scoped table. Each entry: `{ id: "AV-NNN", name: "tenant_isolation:<table>", run: async (db, ctx) => { ... } }`. Vectors at minimum cover: tenants, tenant_members, mcps, oauth_credentials, agents, agent_mcps, agent_tools, agent_skills, agent_triggers, runs, run_steps, audit_log, autonomy_events, knowledge_documents, knowledge_chunks, architect_blueprints, tools, approvals, activity_log, security_findings (the new one from 09-01).
    - Each vector's `run` does THREE things in order: (1) positive control as tenantB userB — assert returns ≥1 row (proves wiring); (2) negative control as tenantA userA — assert returns 0 rows (the actual RLS test); (3) returns `{ id, table, passed: attack.length === 0, actual, expected: 0 }`. If positive control fails, vector throws (the test was broken, not the RLS).
    - `assertVectorsAppendOnly()` reads `REGISTERED_COUNT_FLOOR = ATTACK_VECTORS.length` at module load + exports a check function that throws if anyone deletes entries. PRs that shrink the registry fail this check.
    - `runIsolationSuite(input, opts?)` flow: zod parse → assertVectorsAppendOnly → mkdtemp → loop vectors (`for (const v of ATTACK_VECTORS) results.push(await v.run(opts.db, parsed))`) → write `results.json` to tmpdir → return `{ resultsPath, passed: results.every(r => r.passed), count: results.length }`. NEVER returns raw blobs (CLAUDE.md non-negotiable #4 — large outputs go to file).
    - tsx test (no vitest): asserts (a) zod schema accepts a valid input + rejects empty input, (b) `ATTACK_VECTORS.length >= 20` (the table floor from 0001 + later migrations), (c) `assertVectorsAppendOnly` doesn't throw on the frozen registry, (d) `runIsolationSuite` is an async function. Does NOT require a live DB (live run lives in plan 09-06).
  </behavior>
  <action>
    Create `src/attack-vectors.ts` per the skeleton in PATTERNS.md lines 384-422. One vector per RLS-protected table from migrations 0001 → 0010. Each vector follows the positive-control + attack pattern (Pitfall 5 mitigation T-9-01). IDs zero-padded to 3 digits (AV-001, AV-002, ...) — append-only forever.

    For `tenant_members` and `users` tables (where the per-tenant primary key differs), adapt the query shape — read the table's tenant-scoping column from `packages/db/src/schema.ts` to confirm the join. When in doubt, follow `is_tenant_member` semantics: read by ID as tenantA → expect 0 rows; same read as tenantB → expect 1 row.

    Create `src/index.ts` per PATTERNS.md lines 304-336. Export everything: `runIsolationSuite`, `asUser`, `ATTACK_VECTORS`, `assertVectorsAppendOnly`, `IsolationInput`, `IsolationResult`.

    Create `src/tool-rls-test.test.ts` per PATTERNS.md lines 431-456. Use the bespoke `assert` + counters + `main()` + `process.exit(failed > 0 ? 1 : 0)` pattern (S5). Live-DB tests defer to plan 09-06's `scripts/verify/isolation-live.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @agent-os/tool-rls-test typecheck</automated>
    <automated>pnpm --filter @agent-os/tool-rls-test test</automated>
    <automated>node -e "import('./packages/tool-rls-test/src/attack-vectors.ts').catch(()=>{}); const c = require('fs').readFileSync('packages/tool-rls-test/src/attack-vectors.ts','utf8'); if (!/Object\.freeze/.test(c)) { console.error('not frozen'); process.exit(1); } const ids = c.match(/AV-\d{3}/g) || []; if (ids.length < 20) { console.error('only', ids.length, 'vectors — need >= 20 RLS tables'); process.exit(1); } console.log('OK:', ids.length, 'vectors')"</automated>
    <automated>grep -c 'positive control\|posCtl' packages/tool-rls-test/src/attack-vectors.ts</automated>
  </verify>
  <done>Package typechecks; tsx test passes; ATTACK_VECTORS frozen with ≥20 entries; positive-control language present in source (Pitfall 5 mitigation).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RLS_TEST_DATABASE_URL connection role | Critical: must be `authenticated`-role (RLS-enforced), NOT service-role (RLS-bypass). D-01 enforced via separate env var. |
| ATTACK_VECTORS shrink → false isolation pass | Append-only guard `assertVectorsAppendOnly` makes shrinkage observable; CI catches removals. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-01 | Information Disclosure | RLS test via wrong role | mitigate | Separate `RLS_TEST_DATABASE_URL` env var (D-01) — README documents requirement; live verification script (09-06) hard-fails when env unset; every vector includes a positive control (Pitfall 5) so a service-role connection would produce posCtl-with-attack-also-1 → assertion failure. |
| T-9-10 | Tampering | shrink ATTACK_VECTORS to make suite pass | mitigate | `assertVectorsAppendOnly` runs on every invocation; PRs deleting vectors fail in test + at runtime. |
| T-9-11 | Repudiation | positive control silently regresses | mitigate | Each vector throws (not just returns failed:true) if positive control returns 0 — distinguishes "test broken" from "RLS broken." |
| T-9-SC | Tampering | npm package install | mitigate | All deps (drizzle-orm, zod, @types/node, tsx, typescript) already present in `packages/tool-browser/package.json` → already audited in earlier phases. `@agent-os/db` is workspace-internal. No new external packages — Package Legitimacy Gate satisfied. |
</threat_model>

<verification>
- `pnpm install` succeeds; new package linked into workspace via existing `packages/*` glob (no pnpm-workspace.yaml edit needed)
- `pnpm --filter @agent-os/tool-rls-test typecheck` green
- `pnpm --filter @agent-os/tool-rls-test test` green
- `pnpm -r typecheck` from repo root still green (no cross-package break)
- README mentions `RLS_TEST_DATABASE_URL` + `service-role` (D-01 + Pitfall 1 citation)
- ATTACK_VECTORS has ≥20 entries (one per RLS-protected table)
</verification>

<success_criteria>
- SC-9-2 partially advanced: `tool.rls-test` engine exists (registry seed + dispatch land in 09-04).
- SC-9-4 partially advanced: positive+negative attack vectors compiled (live run lands in 09-06).
- Package is independently testable, importable, and ready for runner dispatch.
</success_criteria>

<output>
Create `.planning/phases/09-tenant-isolation-tester-secrets-rotation-external-launch-gat/09-02-SUMMARY.md` documenting: package name, exported surface, the GUC literal used (`request.jwt.claim.sub`), the count of ATTACK_VECTORS shipped, and the D-01 env var requirement consumers must satisfy.
</output>
