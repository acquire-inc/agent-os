---
phase: 09-tenant-isolation-tester-secrets-rotation-external-launch-gat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0010_security_findings.sql
  - packages/db/src/schema.ts
  - packages/core/src/security/findings.ts
  - packages/core/src/security/findings.test.ts
  - packages/core/src/index.ts
  - packages/core/src/architect/hydrate.ts
autonomous: true
requirements: [SC-9-3, SC-9-5]
must_haves:
  truths:
    - "security_findings table exists in migration 0010 with tenant-scoped RLS"
    - "Drizzle schema mirrors security_findings columns and exports a securityFindings table object"
    - "recordFinding(db, args) inserts one row and returns it"
    - "CANT_FAIL_KEYS in hydrate.ts contains secrets-rotation (the missing key per gap audit)"
  artifacts:
    - path: supabase/migrations/0010_security_findings.sql
      provides: "security_findings DDL + RLS + updated_at trigger + audit_log index"
      contains: "create table if not exists security_findings"
    - path: packages/db/src/schema.ts
      provides: "securityFindings drizzle table mirroring 0010"
      contains: "securityFindings"
    - path: packages/core/src/security/findings.ts
      provides: "recordFinding(db, args) insert helper"
      exports: ["recordFinding", "FindingArgs"]
    - path: packages/core/src/architect/hydrate.ts
      provides: "CANT_FAIL_KEYS extended with secrets-rotation"
      contains: "secrets-rotation"
  key_links:
    - from: packages/core/src/security/findings.ts
      to: security_findings
      via: drizzle sql tagged template insert
      pattern: "insert into security_findings"
    - from: packages/core/src/architect/hydrate.ts
      to: CANT_FAIL_KEYS
      via: Set entry
      pattern: "\"secrets-rotation\""
---

<objective>
Lay the data substrate for Phase 9: migration 0010 introducing `security_findings` (the durable artifact every Phase-9 agent writes into), its Drizzle mirror, a single insert helper (`recordFinding`), and the CANT_FAIL_KEYS gap fix (`secrets-rotation` is missing from the existing set per the pattern-mapper gap call-out).

Purpose: every downstream Phase-9 plan (security modules in Wave 2; runner dispatch + agent seeds in Wave 3) consumes one or both of these — `recordFinding` for write-back, `security_findings` for query/aggregation, CANT_FAIL_KEYS for the architect refusal test.

Output: One new migration, one extended Drizzle schema, one new core module (+ test), and a one-line addition to hydrate.ts.
</objective>

<execution_context>
@/home/user/agent-os/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/agent-os/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/user/agent-os/.planning/phases/09-tenant-isolation-tester-secrets-rotation-external-launch-gat/09-RESEARCH.md
@/home/user/agent-os/.planning/phases/09-tenant-isolation-tester-secrets-rotation-external-launch-gat/09-PATTERNS.md
@/home/user/agent-os/.planning/phases/09-tenant-isolation-tester-secrets-rotation-external-launch-gat/09-CONTEXT.md
@/home/user/agent-os/CLAUDE.md

<interfaces>
<!-- Existing shapes the executor depends on -->

From supabase/migrations/0001_init.sql (lines 36-40, 74-80):
- `auth.uid()` reads `current_setting('request.jwt.claim.sub', true)` → uuid
- `is_tenant_member(t uuid)` SECURITY DEFINER → bool — the universal RLS guard

From supabase/migrations/0006_architect_blueprints.sql (the template to copy):
- Tenant-scoped table + RLS policy + updated_at trigger + tenant index pattern

From packages/db/src/schema.ts:
- Existing tenant-scoped tables follow pattern: `pgTable("name", { id: uuid(...).primaryKey().defaultRandom(), tenantId: uuid("tenant_id").notNull().references(() => tenants.id, { onDelete: "cascade" }), ... })`
- See `auditLog` (line 353) and `autonomyEvents` (line 342) for jsonb payload + timestamptz columns

From packages/core/src/lifecycle.ts (appendActivity lines 56-62):
- Single-table insert helper shape — accepts `Db` + typed args, returns the inserted row via `returning *`

From packages/core/src/architect/hydrate.ts (lines 21-35):
- `const CANT_FAIL_KEYS = new Set([...])` — current set is MISSING `secrets-rotation`; other 3 Phase-9 agent keys are already present
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write migration 0010_security_findings.sql + Drizzle mirror + index re-export</name>
  <files>supabase/migrations/0010_security_findings.sql, packages/db/src/schema.ts</files>
  <read_first>
    - supabase/migrations/0006_architect_blueprints.sql (full file — the exact analog per PATTERNS.md S2/S3)
    - supabase/migrations/0001_init.sql lines 36-40 (auth.uid GUC) and 74-80 (is_tenant_member SECURITY DEFINER)
    - packages/db/src/schema.ts lines 340-360 (auditLog + autonomyEvents — the closest column shape analog)
    - .planning/phases/09-tenant-isolation-tester-secrets-rotation-external-launch-gat/09-PATTERNS.md lines 206-262 (the full migration skeleton)
  </read_first>
  <behavior>
    - Migration creates `security_findings` table with: id uuid PK, tenant_id uuid (FK tenants, cascade), agent_id uuid (FK agents, set null), category text check in ('isolation','rotation','access','anomaly'), severity text check in ('low','medium','high','critical'), status text default 'open' check in ('open','acknowledged','resolved','suppressed'), title text, payload jsonb default '{}', detected_at/acknowledged_at/resolved_at/created_at/updated_at timestamptz.
    - Creates index `security_findings_tenant_status_idx` on (tenant_id, status, severity, detected_at desc).
    - Creates index `audit_log_tenant_ts_idx` on audit_log (tenant_id, ts desc) IF NOT EXISTS (Pitfall 3 mitigation — windowed anomaly scans).
    - Enables RLS + policy `security_findings_rw` using `is_tenant_member(tenant_id)` for both `using` and `with check`.
    - Adds `security_findings_touch_updated_at` plpgsql function + before-update trigger.
    - Drizzle schema export `securityFindings` mirrors every column with correct types; jsonb payload typed as `jsonb("payload").$type<Record<string, unknown>>().notNull().default({})`.
    - All four tenant-scoped enums (category/severity/status) are typed via `.$type<"isolation" | ...>()` on text columns (matching how existing schema does enums via text + check constraint).
  </behavior>
  <action>
    Copy `supabase/migrations/0006_architect_blueprints.sql` verbatim as the structural template. Replace identifiers per the skeleton in 09-PATTERNS.md lines 206-262 (D-05 — single `security_findings` table; positive-control evidence lives in payload, not a separate column, per D-08 marker in the skeleton comment). DO NOT add an `isolation_test_results` table (A6: scope kept tight — findings are the single artifact). Add the `audit_log_tenant_ts_idx` index in the same migration to mitigate Pitfall 3 ahead of `anomaly.ts` arriving in Wave 2.

    Extend `packages/db/src/schema.ts` with the `securityFindings` pgTable. Mirror columns exactly. Place the export alphabetically among the security/audit tables (next to `auditLog` and `autonomyEvents`). Add `securityFindings` to the schema export object at the bottom of the file (follow existing pattern — every table is re-exported).

    Per D-08 + schema_push_requirement: do NOT attempt `supabase db push` in this task. The migration file is the deliverable; the push is a [BLOCKING] operator checkpoint in plan 09-06. Precedent: Phase 7 deferred 0007 + 0008 the same way.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const sql = fs.readFileSync('supabase/migrations/0010_security_findings.sql','utf8'); const checks = [['create table.*security_findings',/create table if not exists security_findings/i],['rls enabled',/enable row level security/i],['rls policy',/create policy security_findings_rw[\s\S]*is_tenant_member/i],['updated_at trigger',/create trigger security_findings_touch/i],['audit_log index',/audit_log_tenant_ts_idx/i],['category check',/category.*check.*isolation.*rotation.*access.*anomaly/is],['severity check',/severity.*check.*low.*medium.*high.*critical/is]]; let ok=true; for (const [n,re] of checks) { if (!re.test(sql)) { console.error('MISSING:', n); ok=false; } else { console.log('OK:', n); } } process.exit(ok?0:1);"</automated>
    <automated>pnpm --filter @agent-os/db typecheck</automated>
  </verify>
  <done>Migration file exists with all 7 grep gates green; Drizzle `securityFindings` exports cleanly; package typechecks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add recordFinding helper + test, fix CANT_FAIL_KEYS, re-export from core</name>
  <files>packages/core/src/security/findings.ts, packages/core/src/security/findings.test.ts, packages/core/src/index.ts, packages/core/src/architect/hydrate.ts, packages/core/package.json</files>
  <read_first>
    - packages/core/src/lifecycle.ts lines 56-62 (appendActivity — the exact insert-helper analog per PATTERNS.md)
    - packages/core/src/architect/hydrate.ts lines 21-35 (CANT_FAIL_KEYS — the set to extend)
    - packages/core/src/index.ts (current public re-exports — add the findings module to the same pattern)
    - packages/tool-browser/src/tool-browser.test.ts lines 7-77 (the tsx-only test harness pattern S5 — no vitest)
    - packages/core/package.json (scripts.test entries — add a `test:security` script following the existing `test:architect` convention)
    - .planning/phases/09-tenant-isolation-tester-secrets-rotation-external-launch-gat/09-PATTERNS.md lines 571-597 (the findings.ts shape) and 638-651 (the CANT_FAIL_KEYS extension)
  </read_first>
  <behavior>
    - `recordFinding(db, args)` accepts `{ tenantId, agentId?, category, severity, title, payload? }` and inserts one row via drizzle `sql` tagged template; returns the inserted row.
    - `category` is a string-literal union of `"isolation" | "rotation" | "access" | "anomaly"`; severity is `"low" | "medium" | "high" | "critical"`. TypeScript MUST reject other strings at compile time.
    - Test (tsx, no vitest): shape-validates the FindingArgs type at compile time + asserts the function is exported; does NOT require a live DB (insertion path is exercised in Wave 2 modules + Wave 3 verification).
    - `CANT_FAIL_KEYS` set in hydrate.ts contains `"secrets-rotation"` after the change. Other Phase-9 keys (tenant-isolation-tester, security-anomaly-watchdog, access-auditor) are already present — leave untouched.
    - `packages/core/src/index.ts` re-exports `recordFinding`, `FindingArgs` from `./security/findings.js`.
    - `packages/core/package.json` gains a `"test:security": "tsx src/security/*.test.ts"` script entry under `scripts`.
  </behavior>
  <action>
    Create `packages/core/src/security/findings.ts` per PATTERNS.md skeleton (lines 571-597). Import `sql` from drizzle-orm, `Db` from `@agent-os/db`. Export `FindingArgs` interface + `recordFinding` async function.

    Create `packages/core/src/security/findings.test.ts` following S5 (counters + assert + main + process.exit). Cover: (a) `recordFinding` is a function, (b) FindingArgs typecheck passes at compile time for valid args, (c) reject invalid category at type level via `@ts-expect-error` (compile-time only — the test BODY just asserts the file compiled).

    Modify `packages/core/src/architect/hydrate.ts` lines 23-25 — add `"secrets-rotation",` to the `CANT_FAIL_KEYS` Set, preserving alphabetical-ish ordering used in the existing block (slot it between `security-anomaly-watchdog` and other security agents). Cite the gap fix in a comment: `// Phase 9: secrets-rotation added per CLAUDE.md can't-fail list (pattern-mapper gap fix)`.

    Re-export from `packages/core/src/index.ts` — add `export { recordFinding, type FindingArgs } from "./security/findings.js";` next to the existing security/auth re-exports.

    Add `"test:security": "tsx src/security/*.test.ts"` to `packages/core/package.json` scripts (mirroring `test:architect`).
  </action>
  <verify>
    <automated>pnpm --filter @agent-os/core typecheck</automated>
    <automated>pnpm --filter @agent-os/core run test:security</automated>
    <automated>grep -v '^//\|^#' packages/core/src/architect/hydrate.ts | grep -c '"secrets-rotation"'</automated>
    <automated>node -e "const c = require('fs').readFileSync('packages/core/src/index.ts','utf8'); if (!/recordFinding/.test(c)) { console.error('not re-exported'); process.exit(1); } console.log('OK')"</automated>
  </verify>
  <done>Typecheck green; new test runs and exits 0; grep returns ≥1 for "secrets-rotation" in hydrate.ts (non-comment line); recordFinding is exported from package index.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application → security_findings writer | recordFinding is reachable from any agent context; payload is jsonb (operator-controlled content can land here via Wave 2/3 tools) |
| tenant A session → tenant B's findings | RLS gates every read/write; this plan introduces the policy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-01 | Information Disclosure | security_findings RLS | mitigate | Policy `security_findings_rw` uses `is_tenant_member(tenant_id)` for both USING and WITH CHECK clauses — mirrors the verified pattern from 0006. Negative-control test for this table lives in plan 09-02 (tool-rls-test attack vector AV-N for security_findings). |
| T-9-08 | Tampering | findings payload | accept | jsonb payload is operator-trusted content (only Phase-9 agents write); no SQL injection vector since drizzle parameterizes the literal. Per V8 ASVS: payload MUST NOT contain raw OAuth tokens — enforced by reviewer of Wave-2 callers. |
| T-9-09 | Repudiation | CANT_FAIL_KEYS tampering | mitigate | architect.test.ts assertion (extended in plan 09-04) locks `secrets-rotation` ∈ set as a regression; PRs removing it fail CI. |
| T-9-SC | Tampering | npm/pnpm installs | mitigate | NO new external packages added in this plan (verified — Package Legitimacy Audit in 09-RESEARCH is empty). |
</threat_model>

<rollback>
## Migration 0010 rollback (operator-runnable; this plan does NOT push)

```sql
-- Reverse of 0010_security_findings.sql
drop trigger if exists security_findings_touch on security_findings;
drop function if exists security_findings_touch_updated_at();
drop index if exists security_findings_tenant_status_idx;
-- audit_log_tenant_ts_idx — keep (mitigation worth keeping even if 0010 rolls back);
-- if a clean revert is required: drop index if exists audit_log_tenant_ts_idx;
drop table if exists security_findings;
```

## Drizzle schema rollback
- `git revert` the schema.ts hunk introducing `securityFindings`.
- Re-export removal in `packages/core/src/index.ts` is reverted with the same commit.

## CANT_FAIL_KEYS rollback
- Removing `"secrets-rotation"` from the set is a 1-line revert. Architect tests added in 09-04 will fail until reverted too — intentional regression lock.
</rollback>

<verification>
- `pnpm -r typecheck` green
- Migration file passes the grep gates above (table, RLS, policy with is_tenant_member, trigger, audit_log index, category/severity checks)
- `pnpm --filter @agent-os/core run test:security` green
- `grep -v '^//\|^#' packages/core/src/architect/hydrate.ts | grep -c '"secrets-rotation"'` returns ≥ 1
- No `supabase db push` attempted (deferred to operator per schema_push_requirement)
</verification>

<success_criteria>
- SC-9-3 advanced: migration 0010 written and Drizzle schema mirrors it (push deferred to operator in 09-06).
- SC-9-5 partially advanced: `secrets-rotation` now in CANT_FAIL_KEYS (architect regression lock land in 09-04).
- No downstream plan must edit migration 0010 or hydrate.ts again.
</success_criteria>

<output>
Create `.planning/phases/09-tenant-isolation-tester-secrets-rotation-external-launch-gat/09-01-SUMMARY.md` documenting: the migration filename, the new Drizzle export, the recordFinding signature, the CANT_FAIL_KEYS line edited (with before/after), and the fact that operator must run `supabase db push` (tracked in 09-06).
</output>
