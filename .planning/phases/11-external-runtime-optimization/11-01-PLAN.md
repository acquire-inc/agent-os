---
phase: 11-external-runtime-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/plans/EXTERNAL-RUNTIME-RECONCILIATION.md
  - docs/plans/EXTERNAL-TEMPLATES-AUDIT.md
autonomous: true
requirements: [SC-11-1, SC-11-2]
must_haves:
  truths:
    - "EXTERNAL-RUNTIME-RECONCILIATION.md lands with both forks (Hermes runtime-vs-model, isolation model) and BOTH marked 'DECISION: operator's call, do not implement yet'"
    - "EXTERNAL-RUNTIME-RECONCILIATION.md quotes hermes-runtime/config.yaml verbatim on the Nous-says-NOT-tool-call-tuned point (the smoking-gun evidence)"
    - "EXTERNAL-RUNTIME-RECONCILIATION.md recommends Path B (keep Anthropic Agent SDK runner; port gates/skills/KB; demote Hermes-4-*) but does NOT implement it"
    - "EXTERNAL-TEMPLATES-AUDIT.md has three columns: (a) Already have, (b) Should adopt + WHY + WHERE in our architecture, (c) Irrelevant/conflicts"
    - "Audit cross-references AGENTS-PLAN.md canonical anatomy, Relay schema §8 + the 3 new AGENT-OS-PLAN sections (Execution Plane, Self-Improvement Loop, Multi-Dataset Data Moat)"
    - "Audit specifically calls out the 5 categories the operator's prompt named: (i) sandboxed/VPS execution + tenant isolation, (ii) eval harnesses/self-improvement/feedback loops, (iii) telemetry/event-schema/'pixel' patterns, (iv) skill/agent registry/MCP-tool patterns, (v) governance/approval/autonomy-tier patterns"
    - "Both docs respect the WRITE BOUNDARY: only acquire-inc/agent-os; the read-only zip stays chmod a-w throughout"
    - "Neither doc modifies CLAUDE.md, the 3 plans, scripts/seed/, external/acqu-skills/, or any code"
  artifacts:
    - path: docs/plans/EXTERNAL-RUNTIME-RECONCILIATION.md
      provides: "Hermes runtime-vs-model fork + isolation fork reconciliation"
      contains: "DECISION: operator's call, do not implement yet"
    - path: docs/plans/EXTERNAL-TEMPLATES-AUDIT.md
      provides: "3-column audit cross-referenced to our planning corpus"
      contains: "AGENTS-PLAN.md anatomy"
  key_links:
    - from: docs/plans/EXTERNAL-RUNTIME-RECONCILIATION.md
      to: hermes-runtime/config.yaml (READ-ONLY at /tmp/ref)
      via: verbatim quote
      pattern: "Nous' OWN docs say their Hermes-4-70b/405b are chat/reasoning-tuned"
    - from: docs/plans/EXTERNAL-TEMPLATES-AUDIT.md
      to: docs/plans/AGENT-OS-PLAN.md §8
      via: cross-reference to Relay schema + canonical event namespace
      pattern: "AGENT-OS-PLAN.md §8"
    - from: docs/plans/EXTERNAL-TEMPLATES-AUDIT.md
      to: docs/plans/AGENTS-PLAN.md
      via: cross-reference to canonical agent anatomy + per-agent emission contract
      pattern: "AGENTS-PLAN.md anatomy"
---

<objective>
Wave 1: produce the two operator-facing planning docs that consolidate the read-only survey of the external runtime repo and surface the two forks for operator decision. NO code changes, NO live-fleet edits, NO branch creation in this plan — just two doc writes to `docs/plans/`.

The RECONCILIATION doc names the Hermes runtime-vs-model fork (resolved-by-evidence; recommended Path B) + the isolation model fork (per-client VPS+Supabase vs our RLS+single-Supabase; recommended ours). Both marked "operator's call, do not implement yet."

The AUDIT doc puts every salient capability from the external runtime into one of three columns: (a) already-have, (b) should-adopt with WHY + WHERE-it-slots, (c) irrelevant/conflicts. Cross-references our existing planning corpus so the operator can read the audit + our plans side-by-side.

Output: 2 markdown files in `docs/plans/`. Total ~4,600 words. Both committed to the current branch (`claude/exciting-davinci-yvptm`) as documentation.
</objective>

<execution_context>
@/home/user/agent-os/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/agent-os/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/user/agent-os/.planning/phases/11-external-runtime-optimization/11-CONTEXT.md
@/home/user/agent-os/docs/plans/AGENT-OS-PLAN.md
@/home/user/agent-os/docs/plans/AGENTS-PLAN.md
@/home/user/agent-os/docs/plans/GENX-PLAN.md
@/home/user/agent-os/docs/plans/OPTIMIZATION-AUDIT.md

<interfaces>
External (READ-ONLY at `/tmp/ref/agentic-templates-restructure-v2-stack-alignment/`, chmod a-w):
- `hermes-runtime/config.yaml` — verbatim source for the Nous-says-NOT-tool-call-tuned quote
- `hermes-runtime/SOUL.md` — runtime identity layer; the "Opus 4.8 as both model and judge" framing
- `hermes-runtime/AGENTS.md` — isolation model evidence ("per-client Supabase is the source of truth")
- `hermes-runtime/SELF_IMPROVEMENT_AND_GATES_SPEC.md` — A1-A5 + B1-B5 build order (audit column-b adoption map)
- `hermes-runtime/plugins/agentic_gates/__init__.py` — 8-gate regex pattern banks (audit cross-ref to our PreToolUse hook)
- `hermes-runtime/skills/agentic/*/SKILL.md` (20 files) — doctrine skills (audit + extraction source)
- `agentic_build/output_quality_auditor.py` + `rubrics/` — output-quality scorecard rubric structure (audit cross-ref to agent-evaluator scorecard gap)
- `templates/agent-sdk-base/src/{critique,learning}.ts` — pure-functional L2 learning (audit cross-ref)
- `internal-services/retrospective/` — retrospective service shape

Internal (our existing planning corpus — read for cross-references):
- `docs/plans/AGENT-OS-PLAN.md` §8 (Relay schema), Open Q #1 RESOLVED (T-critical → Opus floor), the 3 new sections (Execution Plane, Self-Improvement Loop, Multi-Dataset Data Moat)
- `docs/plans/AGENTS-PLAN.md` (canonical anatomy + per-agent Relay emission contract)
- `docs/plans/GENX-PLAN.md` (CRA blocklist mechanism, free-tier launch-gate predicates)
- `docs/plans/OPTIMIZATION-AUDIT.md` (Tier 1 closed, Tier 2 backlog — agent-evaluator scorecard, embedder, CRA blocklist, reserve/commit, etc.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write `docs/plans/EXTERNAL-RUNTIME-RECONCILIATION.md` (~1,800 words)</name>
  <files>docs/plans/EXTERNAL-RUNTIME-RECONCILIATION.md</files>
  <read_first>
    - /tmp/ref/agentic-templates-restructure-v2-stack-alignment/hermes-runtime/config.yaml (the model: block AND the WHY-NOT-Nous-native comment — verbatim source)
    - /tmp/ref/agentic-templates-restructure-v2-stack-alignment/hermes-runtime/SOUL.md (first 100 lines — the "Opus 4.8 is both your model and your judge" framing)
    - /tmp/ref/agentic-templates-restructure-v2-stack-alignment/hermes-runtime/AGENTS.md ("per-client Supabase is the source of truth" sentence)
    - docs/plans/AGENT-OS-PLAN.md (find Open Q #1 RESOLVED — the existing T-critical → Opus floor that survives both forks)
    - packages/core/src/router/tier-models.ts (the current DEFAULT_TIER_MODELS — what would change under Path B if operator approves later)
    - .planning/phases/11-external-runtime-optimization/11-CONTEXT.md (this phase's locked decisions)
  </read_first>
  <action>
    Write the file with this required shape:
    1. **Header**: snapshot SHA placeholder (`<commit SHA from /tmp/ref tarball — operator fills>`), `License: NONE (all rights reserved by default — reauthor never copy)`, `Survey commit: 2026-06-02`, `Status: DECISION: operator's call, do not implement yet`
    2. **Fork A — Hermes runtime-vs-model** (~1,000 words):
       - State the contradiction in 1 paragraph: our `DEFAULT_TIER_MODELS["T-reason"].primary = "nousresearch/hermes-4-405b"` vs the runtime's design
       - Quote the runtime's `config.yaml` VERBATIM (the "WHY NOT a Nous-native model" comment block — the smoking gun)
       - Lay out Path A (adopt their Hermes runtime: "Hermes inside itself") vs Path B (keep our Anthropic Agent SDK runner; port gates/skills/KB; demote Hermes-4-*)
       - Cost-benefit table: what each path preserves, what each unwinds
       - **Note the T-critical → Opus 4.8 floor is correct under BOTH paths** — the contested half is only "Hermes-4-405B as the non-critical default" (the slug, not the safety contract)
       - **Recommended: Path B**. Reasoning: Path A unwinds 132,000 lines of session work (Relay spine, Model Router, runtime cantfail assertion, custom-tools dispatch). Path B preserves all of that AND gets the doctrine right on the model side
       - Decision-blocker box: "DECISION: operator's call, do not implement yet. Sub-decision: demote Hermes-4-70b/405b in DEFAULT_TIER_MODELS now, or stage to a follow-up phase?"
    3. **Fork B — Isolation model** (~600 words):
       - Their model: per-client VPS + per-client Supabase (physical isolation)
       - Our model: single Supabase + tenant_id RLS via is_tenant_member() + 32-vector attack registry (the moat thesis depends on this)
       - Side-by-side table: what each model bets on, the cost shape, the moat implications, the audit-trail shape
       - **Recommended: keep ours**. Reasoning: their model would unwind cross-tenant aggregation (the relay_events_xtenant_agg view from AGENT-OS-PLAN §8) which IS the moat thesis. Their physical-isolation floor and our RLS-floor are both legitimate; they're different bets
       - Note the Execution Plane section in AGENT-OS-PLAN is the adjacent design space — ephemeral per-run sandboxed containers can give us closer-to-physical execution isolation without unwinding the DB-layer RLS contract
       - Decision-blocker box: "DECISION: operator's call, do not implement yet."
    4. **What stays intact regardless of either fork's resolution**:
       - The T-critical → Opus 4.8 floor (Open Q #1 RESOLVED — seedAgent exemption + runtime `cantfail.model_violation` assertion)
       - The CRA blocklist mechanism resolution (GENX Open Q #8 — pending implementation)
       - The Architect's CANT_FAIL_KEYS refusal
       - The Relay event spine (28-event closed namespace)
       - All Phase 9 security agents
    5. **Honest acknowledgment**: the license is NONE; the reconciliation doc itself only DESCRIBES the external runtime; nothing is copied. The quoted `config.yaml` paragraph is short, used for fair-use reconciliation purposes, properly attributed
  </action>
  <verify>
    <automated>test -f docs/plans/EXTERNAL-RUNTIME-RECONCILIATION.md && echo "EXISTS"</automated>
    <automated>grep -q "DECISION: operator's call, do not implement yet" docs/plans/EXTERNAL-RUNTIME-RECONCILIATION.md</automated>
    <automated>grep -q "Path A\|Path B" docs/plans/EXTERNAL-RUNTIME-RECONCILIATION.md</automated>
    <automated>grep -q "chat/reasoning-tuned, NOT tool-call-tuned" docs/plans/EXTERNAL-RUNTIME-RECONCILIATION.md</automated>
    <automated>grep -q "License: NONE" docs/plans/EXTERNAL-RUNTIME-RECONCILIATION.md</automated>
    <automated>grep -q "T-critical → Opus\|T-critical \\-\\-> Opus\|T-critical -> Opus" docs/plans/EXTERNAL-RUNTIME-RECONCILIATION.md</automated>
    <automated>wc -w docs/plans/EXTERNAL-RUNTIME-RECONCILIATION.md | awk '{ if ($1 < 1600 || $1 > 2100) { print "FAIL: word count "$1" outside [1600, 2100] — IN-01"; exit 1; } else { print "OK word count "$1; } }'</automated>
    <automated>git diff HEAD -- packages/core/src/router/tier-models.ts | wc -l | awk '{ if ($1 != "0") { print "FAIL: tier-models.ts modified by this task — WR-03"; exit 1; } else { print "OK: tier-models.ts unchanged"; } }'</automated>
  </verify>
  <done>RECONCILIATION.md exists with both forks, verbatim quote, recommendations, operator-decision blockers; no code changes; T-critical floor preserved; license respected.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write `docs/plans/EXTERNAL-TEMPLATES-AUDIT.md` (~2,800 words)</name>
  <files>docs/plans/EXTERNAL-TEMPLATES-AUDIT.md</files>
  <read_first>
    - /tmp/ref/agentic-templates-restructure-v2-stack-alignment/hermes-runtime/SELF_IMPROVEMENT_AND_GATES_SPEC.md (full — the A1-A5 + B1-B5 build order is the column-b adoption spine)
    - /tmp/ref/agentic-templates-restructure-v2-stack-alignment/hermes-runtime/plugins/agentic_gates/__init__.py (the SEND_CLASS + FILE_MUTATION regex banks + safe defaults)
    - /tmp/ref/agentic-templates-restructure-v2-stack-alignment/hermes-runtime/skills/agentic/gate-completion-claim-audit/SKILL.md (the verification-before-completion source doctrine)
    - /tmp/ref/agentic-templates-restructure-v2-stack-alignment/hermes-runtime/skills/agentic/understand-the-client/SKILL.md (the clarify-before-acting source)
    - /tmp/ref/agentic-templates-restructure-v2-stack-alignment/agentic_build/output_quality_auditor.py (the rubric structure)
    - /tmp/ref/agentic-templates-restructure-v2-stack-alignment/agentic_build/retro_runner.py (the cron-invokable retrospective pattern)
    - /tmp/ref/agentic-templates-restructure-v2-stack-alignment/templates/agent-sdk-base/src/critique.ts (the pure L1-critique pattern)
    - /tmp/ref/agentic-templates-restructure-v2-stack-alignment/templates/agent-sdk-base/src/learning.ts (the pure L2-learning pattern)
    - /tmp/ref/agentic-templates-restructure-v2-stack-alignment/hermes-runtime/AGENTS.md (3-tier memory architecture — per-client Supabase + Hermes-native + Holographic)
    - docs/plans/AGENT-OS-PLAN.md §8 (Relay schema for cross-references)
    - docs/plans/AGENTS-PLAN.md (canonical anatomy for cross-references)
    - docs/plans/OPTIMIZATION-AUDIT.md (the Tier 2 backlog the audit-adoption items map into)
    - apps/runner/src/hooks.ts (the PreToolUse hook where the 8-gate pattern banks would slot)
  </read_first>
  <action>
    Write the file with this required shape:
    1. **Header**: snapshot SHA placeholder, `License: NONE (all rights reserved — reauthor never copy)`, `Survey commit: 2026-06-02`
    2. **Cross-reference key** (1 paragraph): explain that every column-b row carries WHERE it slots in our architecture using specific file paths to the existing planning corpus
    3. **Column (a) — Already have** (~600 words; table form):
       - T-critical → Opus 4.8 floor (their Opus-as-judge ↔ our Open Q #1 RESOLVED runtime assertion)
       - PreToolUse autonomy gate ↔ their pre_tool_call hook
       - CANT_FAIL_KEYS ↔ their gates-as-doctrine
       - Cost ceiling per run ↔ their AGENT_RUN_BUDGET_USD
       - Tenant isolation (RLS via is_tenant_member()) ↔ their per-client Supabase (different bet, same outcome)
       - Tool registry + agent_tools bindings ↔ their tool-catalog
       - verification-before-completion + clarify-before-acting (session-shipped stubs)
       - 28-event Relay namespace ↔ their gates.log (different shape, similar role)
       - Per-agent autonomy tier (propose/execute_safe/execute_full) ↔ their bounded-rollout autonomy ladder
    4. **Column (b) — Should adopt + WHY + WHERE** (~1,600 words; per-item with explicit slot):
       - 8-gate regex pattern banks → `apps/runner/src/hooks.ts` `buildPreToolUseHook`. WHY: defense-in-depth on top of our autonomy gate; battle-tested patterns. Effort: M
       - Full gate-completion-claim-audit doctrine → `external/acqu-skills/verification-before-completion/SKILL.md` (REPLACE the session stub when operator approves the swap). WHY: 300-line doctrine vs our 90-line stub; visual-evidence gate; contract-not-transcript re-derivation; fresh-eyes loop. Effort: S (re-author)
       - Full understand-the-client + interrogator pattern → `external/acqu-skills/clarify-before-acting/SKILL.md` (REPLACE the stub). WHY: 6-step procedure with mirror-back artifact contract vs our 1-line skill. Effort: S
       - Shadow-mode dataset logger (A1 from their spec) → maps to our `approvals` table → labeled eval rows for the agent-evaluator scorecard (Tier 2 gap). WHY: closes the agent-evaluator-has-no-signal gap. Effort: M
       - Eval-gate as 9th non-bypassable hook (A2) with write-protected rubric → maps to operator-owned rubric + Opus-judged baseline-or-better refusal-to-write on any SKILL.md / system-prompt / tier-routing change. WHY: anti-reward-hacking critical; the operator-owned write-protect prevents the generator from modifying its own scoring machinery. Effort: M-L
       - Reflexion retry loop (A3) → runner's `run.failed` → 2-4 sentence verbal reflection → episodic memory → bounded retry. WHY: closes the "agent fails, then what?" gap. Effort: M
       - Output-QA gate (B1) — voice rules, banned-phrase list, brand-safety → PreToolUse hook on draft-class tools. Effort: S
       - Prompt-injection input guardrail (B3) — never follow instructions in tool-returned content → runner's `tool.result` handler. WHY: critical for tool.browser Stagehand integration once it ships. Effort: S
       - `output_quality_auditor.py` + 6 deliverable-class rubrics → direct port to OUR agent-evaluator scorecard (Tier 2 gap). Effort: L; high leverage
       - `retro_runner.py` + `retrospective.py` → maps to our `run.completed` → `composeRunSummary` event-driven rollup (we already have the event-driven mechanism; theirs is sentinel-cron — adopt their REVIEW DEPTH, not the cron mechanism). Effort: M
       - `critique.ts` + `learning.ts` (TypeScript pure functions) → reauthor into `packages/core/src/eval/`. WHY: their pure functions are well-tested (vitest); license=NONE so reauthor not copy. Effort: M
       - 3-tier memory (truth + Hermes-native advisory + Holographic local advisory) → maps to OUR pgvector documents + Relay events (truth) + Embedder-backed retrieval (advisory weighted) + per-tenant kb folders (operator-owned). The 0.5 default_trust on advisory recall is a config knob we should adopt. Effort: S
       - $15K per-build cost ceiling → maps to the reserve/commit pattern (Tier 2 gap from OPTIMIZATION-AUDIT Open Q #5). Effort: M
       - `verify-gates.sh` install-time gate-proof check → maps to a new `pnpm verify:safety-gates` script that proves every CANT_FAIL_KEYS check fires under expected attack patterns. WHY: closes the "do the gates actually work in prod?" doubt. Effort: S
       - Knowledge-base structure (`lessons/`, `known_issues/`, `playbooks/`) → adopt the three categories for any future kb seeding. Effort: S
    5. **Column (c) — Irrelevant or conflicts** (~400 words):
       - Their per-client VPS + per-client Supabase (their isolation choice; conflicts with our RLS+single-Supabase moat thesis)
       - `hermes` CLI + `hermes auth add nous` flow (their runtime; we use Claude Agent SDK)
       - `managed-agents-registry.json` of ~70 Anthropic Managed Agents (different agent-execution model; we use our own runner)
       - `website-factory/` (client-facing build factory — out of scope per hard-exclude)
       - Their Telegram heartbeat / Loom watching (client-specific ops; not generic OS work)
    6. **Explicit category cross-reference** (the operator's prompt called these out specifically — separate block):
       - (i) sandboxed/VPS execution + tenant isolation — see Fork B + column-(b) Execution Plane mapping
       - (ii) eval harnesses / self-improvement / feedback loops — see column-(b) Shadow-mode + A2-A5 + output_quality_auditor + critique/learning rows; cross-references AGENT-OS-PLAN §A2
       - (iii) telemetry/event-schema or dataset/"pixel" patterns — their `gates.log` ↔ our 28-event Relay namespace; their 3-tier memory ↔ our Relay/run_summaries + pgvector; cross-references AGENT-OS-PLAN §8
       - (iv) skill/agent registry or MCP-tool patterns — their `tool-catalog` skill + `provision-manifest.yaml` ↔ our tools registry + future tenant onboarding (GenX work)
       - (v) governance/approval/autonomy-tier patterns — their 8 gates + bounded-rollout ladder ↔ our autonomy tiers + CANT_FAIL_KEYS + the Approvals inbox
    7. **Honest note**: column-(b) row count exceeds what Phase 11 will extract. The 8 quarantined candidates for Wave 2 are a SUBSET; the remaining column-(b) items go into future phases (agent-evaluator scorecard, Tier 2 work)
  </action>
  <verify>
    <automated>test -f docs/plans/EXTERNAL-TEMPLATES-AUDIT.md && echo "EXISTS"</automated>
    <automated>grep -qE "Column \(a\)|## .a. Already have" docs/plans/EXTERNAL-TEMPLATES-AUDIT.md</automated>
    <automated>grep -qE "Column \(b\)|## .b. Should adopt" docs/plans/EXTERNAL-TEMPLATES-AUDIT.md</automated>
    <automated>grep -qE "Column \(c\)|## .c. Irrelevant" docs/plans/EXTERNAL-TEMPLATES-AUDIT.md</automated>
    <automated>grep -q "AGENTS-PLAN.md" docs/plans/EXTERNAL-TEMPLATES-AUDIT.md</automated>
    <automated>grep -q "AGENT-OS-PLAN.md" docs/plans/EXTERNAL-TEMPLATES-AUDIT.md</automated>
    <automated>grep -qE "sandboxed.VPS execution|sandboxed/VPS" docs/plans/EXTERNAL-TEMPLATES-AUDIT.md</automated>
    <automated>grep -qE "eval harness|self-improvement|feedback loop" docs/plans/EXTERNAL-TEMPLATES-AUDIT.md</automated>
    <automated>grep -qE "telemetry|event-schema|pixel" docs/plans/EXTERNAL-TEMPLATES-AUDIT.md</automated>
    <automated>grep -qE "MCP|skill registry|tool registry" docs/plans/EXTERNAL-TEMPLATES-AUDIT.md</automated>
    <automated>grep -qE "governance|autonomy|approval" docs/plans/EXTERNAL-TEMPLATES-AUDIT.md</automated>
    <automated>grep -q "License: NONE" docs/plans/EXTERNAL-TEMPLATES-AUDIT.md</automated>
    <automated>wc -w docs/plans/EXTERNAL-TEMPLATES-AUDIT.md | awk '{ if ($1 < 2200 || $1 > 3400) { print "WARN: word count "$1" outside [2200, 3400]"; } else { print "OK word count "$1; } }'</automated>
  </verify>
  <done>EXTERNAL-TEMPLATES-AUDIT.md exists with all 3 columns, cross-referenced to AGENTS-PLAN + AGENT-OS-PLAN, the 5 operator-named categories explicitly called out, license respected.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify zero live-fleet changes + commit both docs</name>
  <files>docs/plans/EXTERNAL-RUNTIME-RECONCILIATION.md, docs/plans/EXTERNAL-TEMPLATES-AUDIT.md</files>
  <read_first>None — verification + commit only.</read_first>
  <action>
    Run these checks; ALL must pass before the commit:

    1. **CR-01 fix — live-fleet name-only diff catches A/M/D/R alike** (NOT just modifications):
       ```bash
       modified_pre=$(git diff HEAD --name-only -- \
         scripts/seed/ external/acqu-skills/ CLAUDE.md \
         docs/plans/AGENT-OS-PLAN.md docs/plans/AGENTS-PLAN.md \
         docs/plans/GENX-PLAN.md docs/plans/OPTIMIZATION-AUDIT.md \
         packages/core/src/router/tier-models.ts \
         packages/core/src/architect/hydrate.ts | wc -l)
       if [ "$modified_pre" -ne 0 ]; then
         echo "FAIL: $modified_pre live-fleet files modified pre-commit"; exit 1
       fi
       echo "OK: zero live-fleet changes pre-commit"
       ```

    2. `git remote -v` — must show only `origin` pointing to `bunn-os/agent-os` (or its mirror); no remote pointing at any external repo
    3. **IN-04 fix — write-attempt probe (chmod is best-effort posture; this asserts actual non-writability)**:
       ```bash
       if touch /tmp/ref/.write_probe 2>/dev/null; then
         rm -f /tmp/ref/.write_probe; echo "FAIL: /tmp/ref writable — IN-04"; exit 1
       fi
       echo "OK: /tmp/ref non-writable"
       ```
    4. The 2 new files exist: `docs/plans/EXTERNAL-RUNTIME-RECONCILIATION.md` and `docs/plans/EXTERNAL-TEMPLATES-AUDIT.md`
    5. The `.planning/phases/11-external-runtime-optimization/` directory exists with `11-CONTEXT.md` + `11-01-PLAN.md` + `11-02-PLAN.md` + `11-03-PLAN.md`

    Then commit on the current branch (`claude/exciting-davinci-yvptm`):
    ```
    git add docs/plans/EXTERNAL-RUNTIME-RECONCILIATION.md docs/plans/EXTERNAL-TEMPLATES-AUDIT.md
    git add .planning/ROADMAP.md
    git add .planning/phases/11-external-runtime-optimization/
    git commit -m "$(cat <<'COMMIT_BODY'
docs(phase-11/01): external runtime reconciliation + audit (planning only, no live-fleet changes)

Phase 11 Wave 1. Two planning docs that consolidate the read-only survey
of wifiwave/agentic-templates-restructure-v2-stack-alignment (read-only at
/tmp/ref, chmod a-w, license=NONE all-rights-reserved).

EXTERNAL-RUNTIME-RECONCILIATION.md:
  Surfaces two operator-decision forks, both marked "DECISION: operator's
  call, do not implement yet":
    Fork A (Hermes runtime-vs-model) — resolved by the runtime's own
      config.yaml which states verbatim that Nous explicitly says
      hermes-4-70b/405b are chat/reasoning-tuned, NOT tool-call-tuned,
      and recommends Claude (sonnet-4.6 / opus) to RUN the Hermes
      framework. Recommended Path B (keep Anthropic Agent SDK runner;
      port gates/skills/KB; demote Hermes-4-* from DEFAULT_TIER_MODELS).
      T-critical → Opus 4.8 floor is correct under BOTH paths.
    Fork B (isolation model) — their per-client VPS + per-client Supabase
      vs our RLS+single-Supabase moat thesis. Recommended: keep ours.

EXTERNAL-TEMPLATES-AUDIT.md:
  3-column audit cross-referenced to AGENTS-PLAN.md canonical anatomy,
  Relay schema §8 + the 3 new AGENT-OS-PLAN sections (Execution Plane,
  Self-Improvement Loop, Multi-Dataset Data Moat).
    Column (a) Already have: T-critical Opus floor, PreToolUse gate,
      CANT_FAIL_KEYS, cost ceiling, RLS isolation, tool registry, 28-event
      Relay namespace, autonomy tiers.
    Column (b) Should adopt: 8-gate regex pattern banks (→ PreToolUse
      defense-in-depth), gate-completion-claim-audit doctrine (→ replace
      session stub), understand-the-client doctrine (→ replace stub),
      shadow-mode dataset (→ agent-evaluator scorecard signal),
      eval-gate 9th hook with write-protected rubric (→ anti-reward-hacking),
      Reflexion retry loop, output-QA gate, prompt-injection guardrail,
      output_quality_auditor + 6 rubrics, retrospective pattern, pure
      critique/learning functions, 3-tier memory with default_trust, $15K
      per-build ceiling (→ reserve/commit Tier 2), verify-gates.sh
      install-time check, knowledge-base 3-category structure.
    Column (c) Irrelevant/conflicts: per-client VPS+Supabase, hermes CLI,
      managed-agents-registry, website-factory, client-specific ops.

  5 operator-named categories explicitly called out: sandboxed/VPS
  execution + tenant isolation, eval harnesses/self-improvement/feedback
  loops, telemetry/event-schema/'pixel' patterns, skill/agent registry/
  MCP-tool patterns, governance/approval/autonomy-tier patterns.

Verification (all green):
  git diff HEAD scripts/seed/ external/acqu-skills/ CLAUDE.md
    docs/plans/AGENT-OS-PLAN.md docs/plans/AGENTS-PLAN.md
    docs/plans/GENX-PLAN.md docs/plans/OPTIMIZATION-AUDIT.md
    packages/core/src/router/tier-models.ts
    packages/core/src/architect/hydrate.ts
                                              EMPTY (no live-fleet changes)
  git remote -v                               bunn-os/agent-os only
  /tmp/ref chmod                              still read-only
  .planning/phases/11-external-runtime-optimization/ created with
    11-CONTEXT.md + 11-{01,02,03}-PLAN.md
  ROADMAP.md updated with Phase 11 entry

License posture: nothing copied. Both docs DESCRIBE the external runtime
+ quote one short paragraph for fair-use reconciliation, properly
attributed. Quarantined extraction (Wave 2) handles the SKILL.md
candidate re-authoring under explicit provenance + license=NONE tags.

Wave 1 of Phase 11 complete. Wave 2 next: stage 8 quarantined candidates
on feat/external-skills-extraction branch under agents/_candidates/.

https://claude.ai/code/session_014jSUmafDT63eSsJ6wNCWU8
COMMIT_BODY
)"
    git push -u origin claude/exciting-davinci-yvptm
    ```

    Do NOT merge. Do NOT switch branches. The 3 planning docs land on the current branch.
  </action>
  <verify>
    <automated>git log -1 --oneline | grep -q "docs(phase-11/01)"</automated>
    <automated>modified=$(git diff HEAD~1 --name-only -- scripts/seed/ external/acqu-skills/ CLAUDE.md docs/plans/AGENT-OS-PLAN.md docs/plans/AGENTS-PLAN.md docs/plans/GENX-PLAN.md docs/plans/OPTIMIZATION-AUDIT.md packages/core/src/router/tier-models.ts packages/core/src/architect/hydrate.ts | wc -l); if [ "$modified" -ne 0 ]; then echo "FAIL: $modified live-fleet files modified — CR-01"; exit 1; fi; echo "OK: zero live-fleet changes (catches A/M/D/R)"</automated>
    <automated>if touch /tmp/ref/.write_probe 2>/dev/null; then rm -f /tmp/ref/.write_probe; echo "FAIL: /tmp/ref writable — IN-04"; exit 1; fi; echo "OK: /tmp/ref non-writable"</automated>
    <automated>git log -1 --name-only | grep -E "docs/plans/EXTERNAL-RUNTIME-RECONCILIATION.md|docs/plans/EXTERNAL-TEMPLATES-AUDIT.md" | wc -l | awk '{ if ($1 < 2) { print "FAIL: docs not in commit"; exit 1; } else { print "OK"; } }'</automated>
    <automated>git remote -v | grep -vE "bunn-os/agent-os|acquire-inc/agent-os" | head -1 | wc -l | awk '{ if ($1 != "0") { print "FAIL: foreign remote detected"; exit 1; } else { print "OK: write boundary maintained"; } }'</automated>
  </verify>
  <done>Both docs committed + pushed on current branch; zero live-fleet changes confirmed via name-only A/M/D/R diff (CR-01); write boundary maintained; /tmp/ref non-writable verified via touch-probe (IN-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| operator → external runtime repo | `/tmp/ref` is chmod a-w; any read-only fetch failure stops the workflow |
| acquire-inc/agent-os write boundary | NO other repo may be cloned, remote-added, committed to, or pushed to |
| live-fleet code surface | Phase 11 Wave 1 is doc-only; no `.ts`/`.sql`/seed-script writes |

## STRIDE Threat Register

| Threat | Category | Mitigation |
|---|---|---|
| T-11-01 | Information Disclosure | The 2 docs only QUOTE short fair-use paragraphs from the external repo; nothing else is copied; license=NONE attribution included |
| T-11-02 | Tampering | Hard scope-fence in `11-CONTEXT.md` REFUSES modifications to scripts/seed/, external/acqu-skills/, CLAUDE.md, the 3 plans, tier-models.ts, hydrate.ts; verify step asserts |
| T-11-03 | Repudiation | Every claim about the external repo cites the source path; the verbatim Hermes quote includes its config.yaml source line |
| T-11-04 | Elevation of Privilege | NO new remote added; commit goes only to current branch on bunn-os/agent-os |
| T-11-05 | Information Disclosure | NO content read from hard-exclude paths (website-factory/runs, dist, *_business.config.json, leads.json, factory_output.csv, knowledge-base/customers, *william_morgan*); pre-task verify |
</threat_model>

<verification>
- Both docs exist at the documented paths
- License posture stated (NONE → reauthor never copy)
- Hermes config.yaml quoted verbatim with attribution
- Both forks marked "operator's call, do not implement yet"
- Audit has 3 columns + cross-reference to AGENTS-PLAN + AGENT-OS-PLAN + the 5 operator-named categories
- Zero live-fleet file modifications (git diff EMPTY for the protected set)
- Single git remote (bunn-os/agent-os)
- /tmp/ref still chmod a-w
- Commit on current branch (`claude/exciting-davinci-yvptm`)
- ROADMAP.md updated with Phase 11
- `.planning/phases/11-external-runtime-optimization/` carries 4 files (11-CONTEXT + 3 PLAN)
</verification>

<success_criteria>
- SC-11-1 advanced: RECONCILIATION.md shipped
- SC-11-2 advanced: AUDIT.md shipped
- SC-11-5 honored: zero live-fleet changes
- SC-11-6 honored: write boundary maintained
</success_criteria>

<output>
Create `.planning/phases/11-external-runtime-optimization/11-01-SUMMARY.md` documenting: the 2 docs shipped (word counts, key recommendations), the 2 operator decisions still pending (Hermes fork + isolation fork), the audit's column-(b) item count vs the Wave 2 extraction subset (8), and the next-up Wave 2 plan.
</output>
