---
phase: 11-external-runtime-optimization
plan: 03
type: checkpoint:human-action
wave: 3
depends_on: ["11-01", "11-02"]
files_modified: []
autonomous: false
requirements: [SC-11-1, SC-11-2, SC-11-3, SC-11-4]
must_haves:
  truths:
    - "Both Wave 1 docs reviewed by operator (EXTERNAL-RUNTIME-RECONCILIATION.md + EXTERNAL-TEMPLATES-AUDIT.md)"
    - "Wave 2 quarantine branch reviewed by operator (8 candidates + SKILLS-EXTRACTION-REPORT.md)"
    - "Operator decisions captured for: (a) Hermes fork resolution (Path A vs Path B), (b) Hermes-4-* slug demotion timing (now vs staged), (c) isolation fork resolution (recommended: keep ours), (d) per-candidate swap/attach decisions"
  artifacts:
    - path: .planning/phases/11-external-runtime-optimization/11-03-DECISIONS.md
      provides: "Operator-recorded decisions for downstream propagation"
      contains: "Hermes fork:"
---

<objective>
Wave 3 is the human-action checkpoint. Phase 11 cannot close — and the downstream phases (Hermes-fork propagation, candidate swap/attach implementations) cannot begin — until the operator records explicit decisions for each fork + each candidate.

This plan does NOT make changes. It produces the decision-capture template (`11-03-DECISIONS.md`) that the operator fills in after reviewing Waves 1+2 outputs. The decisions feed:

- A follow-up phase that propagates the Hermes-fork resolution to `CLAUDE.md` + `packages/core/src/router/tier-models.ts` + `docs/plans/AGENT-OS-PLAN.md` Open Q #1
- A follow-up phase that implements approved swap/attach actions for candidates the operator approves
- The Tier 2 backlog from `docs/plans/OPTIMIZATION-AUDIT.md` (agent-evaluator scorecard, reserve/commit budget, CRA blocklist) which inherits the candidate decisions
</objective>

<execution_context>
@/home/user/agent-os/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/home/user/agent-os/.planning/phases/11-external-runtime-optimization/11-CONTEXT.md
@/home/user/agent-os/docs/plans/EXTERNAL-RUNTIME-RECONCILIATION.md
@/home/user/agent-os/docs/plans/EXTERNAL-TEMPLATES-AUDIT.md
@/home/user/agent-os/docs/plans/SKILLS-EXTRACTION-REPORT.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Operator reviews Wave 1 docs + Wave 2 quarantine</name>
  <what-built>
    Wave 1 shipped two operator-facing planning docs at `docs/plans/`:
    - `EXTERNAL-RUNTIME-RECONCILIATION.md` — Hermes runtime-vs-model fork + isolation fork, both with recommendations marked "DECISION: operator's call"
    - `EXTERNAL-TEMPLATES-AUDIT.md` — 3-column audit of the external runtime vs our planning corpus, cross-referenced

    Wave 2 shipped 8 quarantined SKILL.md candidates on branch `feat/external-skills-extraction` under `agents/_candidates/`, plus the report at `docs/plans/SKILLS-EXTRACTION-REPORT.md`.

    All three artifacts are read-only from this phase's perspective — they describe what COULD change, not what HAS changed. Nothing in the live fleet is modified.
  </what-built>
  <how-to-verify>
    1. Read `docs/plans/EXTERNAL-RUNTIME-RECONCILIATION.md` end-to-end.
       - Focus question 1: Path A (adopt Hermes runtime) vs Path B (keep Anthropic Agent SDK runner, port gates/skills/KB, demote Hermes-4-*). Recommended: Path B. Your call?
       - Focus question 2: if Path B, demote Hermes-4-70b/405b in `DEFAULT_TIER_MODELS` NOW (one follow-up commit), or stage to a separate phase?
       - Focus question 3: isolation fork — keep our RLS+single-Supabase moat thesis, or adopt their per-client physical isolation? Recommended: keep ours.

    2. Read `docs/plans/EXTERNAL-TEMPLATES-AUDIT.md` end-to-end.
       - Skim column-(b) for any "should adopt" items you DISAGREE with — if so, mark them as drop in the decisions doc.

    3. Read `docs/plans/SKILLS-EXTRACTION-REPORT.md`.
       - 4 sets of per-candidate decisions to make:
         (a) `verification-before-completion-v2` — swap the session-shipped stub? (recommended: yes, after operator review of the v2 doctrine)
         (b) `clarify-before-acting-v2` — swap the session-shipped stub? (recommended: yes)
         (c) Per-candidate attach approvals — which candidates attach to which non-T-critical agents, when? (none attach in this phase regardless)
         (d) Any candidate you want DROPPED?

    4. Spot-check the 8 candidates on the branch:
       ```
       git checkout feat/external-skills-extraction
       ls agents/_candidates/
       # read 2-3 SKILL.md files
       git checkout claude/exciting-davinci-yvptm
       ```
       Verify each carries the Provenance block with `license_status: NONE` and `bound_to: NONE`.

    5. Verify the quarantine integrity:
       ```
       git diff origin/claude/exciting-davinci-yvptm feat/external-skills-extraction --name-only
       # Expect ONLY: agents/_candidates/* and docs/plans/SKILLS-EXTRACTION-REPORT.md
       ```

    6. Fill in `.planning/phases/11-external-runtime-optimization/11-03-DECISIONS.md` using the template (Task 2 below produces the template).
  </how-to-verify>
  <resume-signal>
    Type "decisions-recorded" with the path to your filled-in `11-03-DECISIONS.md` (e.g. `decisions-recorded: .planning/phases/11-external-runtime-optimization/11-03-DECISIONS.md`).

    OR

    Type "request-revisions" with a list of specific revisions needed to any of the 3 docs OR any of the 8 candidates — the next agent session adjusts and re-presents.

    OR

    Type "phase-11-deferred" to park the phase entirely and revisit later — no decisions captured, no downstream work begins.
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Generate the decision-capture template `11-03-DECISIONS.md`</name>
  <files>.planning/phases/11-external-runtime-optimization/11-03-DECISIONS.md</files>
  <read_first>
    - docs/plans/EXTERNAL-RUNTIME-RECONCILIATION.md
    - docs/plans/SKILLS-EXTRACTION-REPORT.md
  </read_first>
  <action>
    Write the template file with the following shape — operator fills in the `<your decision>` placeholders:

    ```markdown
    # Phase 11 — Operator Decisions

    **Filled by:** <operator name>
    **Date:** <date>
    **Status:** <pending | recorded>

    ---

    ## Fork A: Hermes runtime-vs-model

    **Recommended:** Path B (keep Anthropic Agent SDK runner; port gates/skills/KB; demote Hermes-4-* from `DEFAULT_TIER_MODELS`)

    **Decision:** <Path A | Path B | other — explain>

    **Sub-decision (if Path B):** demote Hermes-4-70b/405b in `DEFAULT_TIER_MODELS`?
    - <Now (follow-up commit on claude/exciting-davinci-yvptm) | Staged (separate phase) | Keep as fallback only>

    **Rationale:** <1-2 sentences from operator>

    **Propagation list (if Path B + demote-now):**
    - `CLAUDE.md` model tiering table (rewrite to reflect the runtime-not-model framing)
    - `packages/core/src/router/tier-models.ts` `DEFAULT_TIER_MODELS["T-cheap"|"T-reason"]` (demote)
    - `docs/plans/AGENT-OS-PLAN.md` Open Q #1 (add resolution note)
    - 56 existing seed scripts: those currently on hermes-4-70b (9 agents) or hermes-4-405b (5 agents) get reseeded with the new tier-resolution at next `pnpm seed:phase-N`. No script edits required (Model Router re-resolves at seed time).

    ---

    ## Fork B: Isolation model

    **Recommended:** Keep ours (RLS + single Supabase + 32-vector attack registry)

    **Decision:** <Keep ours | Adopt theirs | Hybrid — explain>

    **Rationale:** <1-2 sentences>

    **If Hybrid:** what's the design space? (note: the Execution Plane section in AGENT-OS-PLAN is the adjacent space — ephemeral per-run sandboxed runner containers can give us closer-to-physical execution isolation without unwinding the DB-layer RLS contract)

    ---

    ## Per-candidate decisions (8 candidates)

    For each candidate, record: KEEP-FOR-LATER (stays in `_candidates/`, no swap/attach now), SWAP (replace session stub), ATTACH (wire to specific agents), or DROP.

    | Candidate | Recommended | Operator decision | Notes |
    |---|---|---|---|
    | `verification-before-completion-v2` | SWAP session stub | <KEEP-FOR-LATER \| SWAP \| DROP> | |
    | `clarify-before-acting-v2` | SWAP session stub | <KEEP-FOR-LATER \| SWAP \| DROP> | |
    | `prompt-injection-guardrail` | KEEP-FOR-LATER (attach to tool.browser agents post-Stagehand) | <KEEP-FOR-LATER \| ATTACH \| DROP> | |
    | `output-quality-gate` | ATTACH (creative-studio, client-comms, weekly-report, content-engine) | <KEEP-FOR-LATER \| ATTACH \| DROP> | |
    | `shadow-mode-discipline` | KEEP-FOR-LATER (surface as canonical) | <KEEP-FOR-LATER \| ATTACH \| DROP> | |
    | `cost-ceiling-discipline` | KEEP-FOR-LATER (feeds Tier 2 reserve/commit) | <KEEP-FOR-LATER \| ATTACH \| DROP> | |
    | `scope-lock-discipline` | ATTACH (ad-ops, launcher, content-engine) | <KEEP-FOR-LATER \| ATTACH \| DROP> | |
    | `secret-scan-veto` | ATTACH (cliently.dev advisory) | <KEEP-FOR-LATER \| ATTACH \| DROP> | |

    ---

    ## Hard rules (re-affirmed for downstream phases)

    - [ ] NO candidate is attached to ANY T-critical / CANT_FAIL_KEYS agent in any follow-up phase
    - [ ] Branch `feat/external-skills-extraction` stays unmerged; per-candidate ATTACH/SWAP decisions are applied via separate commits to the working branch in follow-up phases
    - [ ] License: NONE → reauthored never copied. If verbatim copy is detected in any candidate during operator review, that candidate is DROPPED

    ---

    ## Downstream phases unblocked by these decisions

    - **Phase 12 (proposed)** — Hermes-fork propagation (if Path B + demote-now decided): edit CLAUDE.md tier table, edit tier-models.ts DEFAULT_TIER_MODELS, edit AGENT-OS-PLAN Open Q #1, add regression test that any T-cheap or T-reason agent is NOT on Hermes-4-* unless explicit per-agent spec.model override.
    - **Phase 13 (proposed)** — Candidate swap/attach implementations (per-candidate decisions): replace session stubs, attach to specific non-T-critical agents, add tests.
    - **Tier 2 backlog from OPTIMIZATION-AUDIT.md** — agent-evaluator scorecard implementation (inherits the A1-A5 + B1-B5 + critique.ts + learning.ts patterns from EXTERNAL-TEMPLATES-AUDIT column-b adoption), reserve/commit budget pattern (inherits cost-ceiling-discipline), CRA blocklist implementation (independent).
    ```

    Write this file. It is OPERATOR-FILLED in Task 1's resume-signal step.
  </action>
  <verify>
    <automated>test -f .planning/phases/11-external-runtime-optimization/11-03-DECISIONS.md && echo "EXISTS"</automated>
    <automated>grep -q "Fork A: Hermes runtime-vs-model" .planning/phases/11-external-runtime-optimization/11-03-DECISIONS.md</automated>
    <automated>grep -q "Fork B: Isolation model" .planning/phases/11-external-runtime-optimization/11-03-DECISIONS.md</automated>
    <automated>grep -c "KEEP-FOR-LATER" .planning/phases/11-external-runtime-optimization/11-03-DECISIONS.md | awk '{ if ($1 < 8) { print "FAIL: not all 8 candidate rows have placeholder"; exit 1; } else { print "OK: 8 candidate decisions ready"; } }'</automated>
  </verify>
  <done>Decision-capture template ready for operator fill-in.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat | Category | Mitigation |
|---|---|---|
| T-11-11 | Repudiation | All operator decisions recorded in `11-03-DECISIONS.md` (committed to git); no verbal-only changes |
| T-11-12 | Tampering | Wave 3 makes NO live-fleet changes — purely a checkpoint + template generation |
| T-11-13 | Elevation of Privilege | The decisions doc explicitly re-affirms the T-critical exclusion rule before any downstream phase runs |
</threat_model>

<verification>
- `11-03-DECISIONS.md` exists with the operator-fill template
- Operator records decisions via the resume-signal in Task 1
- NO live-fleet changes from this phase
- The 3 Wave 1+2 artifacts (RECONCILIATION, AUDIT, SKILLS-EXTRACTION-REPORT) are reviewable end-to-end
- The quarantine branch is reviewable independently
- Phase 11 closes only after operator records decisions; downstream phases use the decisions as input
</verification>

<success_criteria>
- SC-11-1 through SC-11-4 verified via operator review (Task 1)
- Operator decisions captured in `11-03-DECISIONS.md` (Task 2 template + Task 1 fill-in)
- Phase 11 close-out gated on operator review
</success_criteria>

<output>
After operator returns "decisions-recorded": create `.planning/phases/11-external-runtime-optimization/11-03-SUMMARY.md` capturing the operator's decisions verbatim + the proposed downstream phase identifiers (e.g. Phase 12: Hermes propagation, Phase 13: candidate swap/attach implementations).

After operator returns "request-revisions": file the revision list as `11-03-REVISIONS.md` and re-spawn the Wave 1 or Wave 2 plan as appropriate (don't close the phase).

After operator returns "phase-11-deferred": write `11-03-DEFERRED.md` noting why; close the phase as deferred without action; the branch `feat/external-skills-extraction` remains pushed for later resumption.
</output>
