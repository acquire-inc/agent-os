---
phase: 07-tools-registry-inngest-scheduler-browserbase-tool-browser
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - packages/inngest/package.json
  - packages/inngest/tsconfig.json
  - packages/inngest/src/index.ts
  - packages/inngest/src/client.ts
  - packages/inngest/src/functions/runScheduled.ts
  - packages/inngest/src/inngest.test.ts
autonomous: false
requirements:
  - SC-7-2
user_setup:
  - service: inngest
    why: "Durable scheduler that replaces in-process polling; signing-key auth for inbound webhooks"
    env_vars:
      - name: INNGEST_EVENT_KEY
        source: "Inngest Dashboard → Settings → Event Keys (production)"
      - name: INNGEST_SIGNING_KEY
        source: "Inngest Dashboard → Settings → Signing Keys (production)"
      - name: INNGEST_DEV
        source: "Set to 1 locally to route to inngest-cli dev server; UNSET in prod"
    dashboard_config:
      - task: "Create Inngest app named 'agent-os'"
        location: "Inngest Dashboard → Apps → New"

must_haves:
  truths:
    - "@agent-os/inngest workspace package exports an inngest client and at least one createFunction definition."
    - "runScheduledAgent function inserts a row into runs when an 'aos/agent.scheduled' event with {jobId} fires."
    - "allowedTools is set explicitly on the Inngest function to keep its sandbox tight (Pitfall 5)."
    - "Unit test covers the function's pure DB-write logic without requiring a live Inngest connection."
  artifacts:
    - path: "packages/inngest/package.json"
      provides: "Workspace package with inngest@^4.5.0 dep"
      contains: "@agent-os/inngest"
    - path: "packages/inngest/src/client.ts"
      provides: "Singleton Inngest client with id='agent-os'"
      contains: "new Inngest"
    - path: "packages/inngest/src/functions/runScheduled.ts"
      provides: "createFunction handling aos/agent.scheduled → runs insert"
      contains: "aos/agent.scheduled"
  key_links:
    - from: "client.ts"
      to: "functions/runScheduled.ts"
      via: "inngest.createFunction"
      pattern: "createFunction"
    - from: "inngest.send('aos/agent.scheduled')"
      to: "runs table"
      via: "step.run insert via createDb"
      pattern: "insert into runs"
---

<objective>
Create the new `@agent-os/inngest` workspace package: an Inngest client (id `agent-os`) and the first scheduled function (`runScheduledAgent`) that subscribes to the `aos/agent.scheduled` event and inserts a row into `runs` — durable, retryable, observable. This is the Inngest-side of Sub-feature §2; the pg_cron bridge (Plan 04) calls it, the Hono mount (Plan 04) exposes it.

Purpose: Doctrine §2.1 names Inngest as the orchestrator. The existing in-process `apps/scheduler` continues to work as a fallback (non-Supabase deploys) but Inngest becomes the primary path.
Output: A typed Inngest function the Hono adapter and pg_cron bridge can wire to.
</objective>

<execution_context>
@/home/user/agent-os/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/agent-os/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-tools-registry-inngest-scheduler-browserbase-tool-browser/07-RESEARCH.md
@.planning/phases/07-tools-registry-inngest-scheduler-browserbase-tool-browser/07-PATTERNS.md
@CLAUDE.md
@docs/main-acqu-agent-doctrine.md
@packages/vault/package.json
@packages/vault/tsconfig.json
@apps/scheduler/src/index.ts

<interfaces>
<!-- Workspace package shape extracted from packages/vault verbatim -->

From packages/vault/package.json:
```
{
  "name": "@agent-os/vault",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": { "typecheck": "tsc --noEmit", "test": "tsx src/vault.test.ts" },
  "dependencies": { "@agent-os/db": "workspace:*", "@agent-os/shared": "workspace:*", "drizzle-orm": "^0.45.2" },
  "devDependencies": { "@types/node": "^22.10.5", "tsx": "^4.19.2", "typescript": "^5.7.3" }
}
```

From apps/scheduler/src/index.ts (L17-L22, tick + evaluateDueJobs pattern):
- The pattern that inserts a runs row from a scheduled trigger; runScheduledAgent must do the same work, just via Inngest event instead of an interval.

From RESEARCH.md §Sub-feature §2 (Inngest function definition):
- `inngest.createFunction({ id: 'aos-run-scheduled', retries: 3 }, { event: 'aos/agent.scheduled' }, async ({ event, step }) => { const { jobId } = event.data; await step.run('insert-scheduled-run', async () => { /* insert runs row */ }); })`
- Pitfall 5: explicit `allowedTools` MUST be set on the function definition to keep the sandbox tight (Inngest's analog to the SDK allowedTools — keep the function from invoking arbitrary steps)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [BLOCKING-HUMAN] Verify `inngest` package legitimacy</name>
  <what-built>None yet — this is a pre-install legitimacy gate per the slopcheck protocol. Package: `inngest@^4.5.0`.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/inngest in a browser.
    2. Confirm the package is owned by `@inngest` org, has > 100k weekly downloads, repo is `github.com/inngest/inngest-js`.
    3. Confirm latest version is ≥ 4.5.0 and has NO `postinstall` script (`npm view inngest scripts.postinstall` should be empty/undefined).
    4. Cross-check the doctrine: `docs/main-acqu-agent-doctrine.md` §2.1 explicitly names Inngest.
    5. Approve to proceed with `pnpm add inngest@^4.5.0` in subsequent tasks.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Scaffold packages/inngest workspace (package.json, tsconfig, src/index.ts, src/client.ts)</name>
  <files>packages/inngest/package.json, packages/inngest/tsconfig.json, packages/inngest/src/index.ts, packages/inngest/src/client.ts</files>
  <read_first>
    - packages/vault/package.json (the analog — verbatim shape)
    - packages/vault/tsconfig.json (verbatim)
    - packages/registry/src/index.ts (re-export pattern)
    - .planning/phases/07-tools-registry-inngest-scheduler-browserbase-tool-browser/07-PATTERNS.md (workspace package shape, L261-L306)
  </read_first>
  <behavior>
    - packages/inngest/package.json: name `@agent-os/inngest`, deps include `inngest: ^4.5.0`, `@agent-os/db: workspace:*`, `@agent-os/shared: workspace:*`, `drizzle-orm: ^0.45.2`; devDeps: types, tsx, typescript
    - packages/inngest/tsconfig.json: extends `../../tsconfig.base.json`, noEmit, types ['node'], include ['src']
    - packages/inngest/src/index.ts: re-exports `./client.js` and `./functions/runScheduled.js`
    - packages/inngest/src/client.ts: exports a single `inngest` singleton — `new Inngest({ id: 'agent-os' })` — reads INNGEST_EVENT_KEY and INNGEST_SIGNING_KEY from env when present; supports dev mode via INNGEST_DEV
    - Workspace install succeeds: `pnpm install` resolves the new workspace symlinks
  </behavior>
  <action>
    Copy packages/vault/package.json verbatim and swap `name` to `@agent-os/inngest`. Add `"inngest": "^4.5.0"` to dependencies (alongside existing @agent-os/db, @agent-os/shared, drizzle-orm). Copy packages/vault/tsconfig.json verbatim. Create packages/inngest/src/index.ts with re-exports for `./client.js` and `./functions/runScheduled.js` (the JS extension is the TS-source-as-entry convention this repo uses). Create packages/inngest/src/client.ts that exports `export const inngest = new Inngest({ id: 'agent-os', eventKey: process.env.INNGEST_EVENT_KEY, signingKey: process.env.INNGEST_SIGNING_KEY, isDev: process.env.INNGEST_DEV === '1' })`. Run `pnpm install` from repo root to wire workspace symlinks. Per RESEARCH §Pitfall 4: do NOT set a fake signing key in dev — let the SDK default when INNGEST_DEV=1.
  </action>
  <verify>
    <automated>cd /home/user/agent-os && test -f packages/inngest/package.json && test -f packages/inngest/src/client.ts && pnpm install --frozen-lockfile=false && pnpm --filter @agent-os/inngest typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source: `packages/inngest/package.json` contains `"name": "@agent-os/inngest"` and `"inngest": "^4.5.0"`
    - Source: `packages/inngest/src/client.ts` contains `new Inngest({ id: 'agent-os'`
    - Source: client.ts references `INNGEST_DEV` env (Pitfall 4 — dev mode)
    - Test: `pnpm --filter @agent-os/inngest typecheck` exits 0
    - CLI: workspace symlink resolves — `ls node_modules/@agent-os/inngest` shows the package
  </acceptance_criteria>
  <done>Workspace package compiles and is importable from other packages.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement runScheduledAgent function with explicit allowedTools (Pitfall 5)</name>
  <files>packages/inngest/src/functions/runScheduled.ts</files>
  <read_first>
    - apps/scheduler/src/index.ts (existing tick() + evaluateDueJobs pattern, L17-L22 — the same DB write you're wrapping in an Inngest function)
    - packages/db/src/schema.ts (the `runs` and `jobs` table exports)
    - .planning/phases/07-tools-registry-inngest-scheduler-browserbase-tool-browser/07-RESEARCH.md (Code Examples — Inngest function definition)
    - .planning/phases/07-tools-registry-inngest-scheduler-browserbase-tool-browser/07-PATTERNS.md (L310-L325 — runScheduled pattern)
  </read_first>
  <behavior>
    - Exports `runScheduledAgent = inngest.createFunction({ id: 'aos-run-scheduled', retries: 3 }, { event: 'aos/agent.scheduled' }, handler)`
    - Handler reads `event.data.jobId` and inserts a `runs` row mirroring the SQL from supabase/migrations/0002_pg_cron.sql `aos_job_cron_command` (lines 25-39)
    - The insert is wrapped in `step.run('insert-scheduled-run', async () => {...})` for retry isolation
    - Function-level config sets explicit `allowedTools` (or step concurrency / cancellation that constrain the sandbox) per Pitfall 5
    - Returns `{ jobId, runId }` for observability
    - Throws (and lets Inngest retry) on transient DB errors
  </behavior>
  <action>
    Create packages/inngest/src/functions/runScheduled.ts. Import `inngest` from `../client.js`. Define `export const runScheduledAgent = inngest.createFunction({ id: 'aos-run-scheduled', retries: 3, concurrency: { limit: 10 } }, { event: 'aos/agent.scheduled' }, async ({ event, step }) => { ... })`. Inside the handler: extract `jobId` from `event.data`. Use `step.run('insert-scheduled-run', async () => { const db = createDb(process.env.DATABASE_URL!); /* select from jobs where id=jobId; insert into runs with the same columns the 0002 aos_job_cron_command sets — tenant_id, agent_id, job_id, status='scheduled', trigger_source='schedule', scheduled_for=date_trunc('minute', now()); skip if not exists row check */ return { runId }; })`. Per Pitfall 5: function options MUST narrow the sandbox — set `concurrency.limit` AND if the SDK version supports `allowedTools` (function-level), set it to an explicit allowlist (e.g. `[]` since this function calls no agent tools, just DB inserts). Reuse the `createDb` import from `@agent-os/db` (same as apps/scheduler does). Return `{ jobId, runId }` for observability. Mirror the SQL precisely from 0002 lines 27-37; the only difference is retries are now Inngest-managed.
  </action>
  <verify>
    <automated>test -f packages/inngest/src/functions/runScheduled.ts && grep -q "aos/agent.scheduled" packages/inngest/src/functions/runScheduled.ts && grep -q "retries:" packages/inngest/src/functions/runScheduled.ts && cd /home/user/agent-os && pnpm --filter @agent-os/inngest typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source: file exports `runScheduledAgent`
    - Source: function options contain `retries: 3` and `concurrency`
    - Source: handler contains `event.data` and `step.run`
    - Source: handler inserts into `runs` table (grep for `insert` and `runs` in the SQL/query)
    - Source: function options reference `allowedTools` OR `concurrency.limit` (Pitfall 5 — narrowed sandbox; document choice in inline comment citing Pitfall 5)
    - Test: `pnpm --filter @agent-os/inngest typecheck` exits 0
  </acceptance_criteria>
  <done>Inngest function defined, typed, and ready to be mounted by the Hono adapter (Plan 04).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Unit test for runScheduledAgent DB-write logic</name>
  <files>packages/inngest/src/inngest.test.ts</files>
  <read_first>
    - packages/inngest/src/functions/runScheduled.ts (the function under test)
    - packages/core/src/integration.test.ts (tsx assertion pattern)
    - .planning/phases/07-tools-registry-inngest-scheduler-browserbase-tool-browser/07-VALIDATION.md (task 7-04 expectations)
  </read_first>
  <behavior>
    - Test imports `runScheduledAgent` and invokes the handler directly (Inngest functions are plain async functions — testable without the runtime)
    - Constructs a fake `event` and a minimal `step` shim (`step.run = async (name, fn) => fn()`)
    - Asserts: handler returns `{ jobId, runId }` on success
    - Asserts: a runs row exists for the given jobId after invocation (gated behind DATABASE_URL — bail if unset)
    - Asserts: function metadata (`runScheduledAgent.id`, the configured `id: 'aos-run-scheduled'`) is exported correctly
  </behavior>
  <action>
    Create packages/inngest/src/inngest.test.ts using the tsx assert pattern (copy header from packages/core/src/integration.test.ts L28-L35). Import `runScheduledAgent` from `./functions/runScheduled.js`. Test 1 (no DB needed): assert `runScheduledAgent` exists and has `id === 'aos-run-scheduled'` (Inngest functions expose their config). Test 2 (DB needed — gate on DATABASE_URL): seed a test job row, build a fake event `{ data: { jobId: testJobId }, name: 'aos/agent.scheduled' }` and a shim `step` with `step.run = async (_name, fn) => fn()`, call the handler, assert the runs row exists in the DB for testJobId. Add `"test": "tsx src/inngest.test.ts"` to packages/inngest/package.json scripts. Use a test-only jobId so re-runs don't collide.
  </action>
  <verify>
    <automated>test -f packages/inngest/src/inngest.test.ts && grep -q "runScheduledAgent" packages/inngest/src/inngest.test.ts</automated>
    <human-check>Run `DATABASE_URL=$LOCAL_DB pnpm --filter @agent-os/inngest test` and confirm all assertions pass; without DATABASE_URL, the first assertion (function metadata) still passes and the second is skipped/marked.</human-check>
  </verify>
  <acceptance_criteria>
    - Source: `packages/inngest/src/inngest.test.ts` exists
    - Source: file imports runScheduledAgent and asserts its `id`
    - Source: file uses the shim-step pattern to invoke the handler
    - Source: `packages/inngest/package.json` has `"test"` script pointing to inngest.test.ts
    - Behavior: with DATABASE_URL set, test exits 0 and a runs row is created for the test jobId
  </acceptance_criteria>
  <done>P7-SC2.a covered. Inngest function is unit-tested without depending on live Inngest infra.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Inngest cloud → API | Inbound webhooks from Inngest cross into the Hono API (handled in Plan 04); this plan only defines the package |
| Inngest function → DB | Function runs as a Node process with DATABASE_URL; same trust level as apps/scheduler today |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-02 | Spoofing / Replay | Inngest webhook auth | mitigate | INNGEST_SIGNING_KEY enforced by `inngest/hono` serve handler in Plan 04; this plan ensures the client is constructed with signingKey from env so verification can be wired downstream. Dev-mode bypass gated on INNGEST_DEV=1 (Pitfall 4 — never set a fake signing key in dev). |
| T-7-09 | Sandbox escape | Inngest function with wide-open allowedTools | mitigate | Pitfall 5: function options narrow the sandbox via `concurrency.limit` and (when SDK exposes it) `allowedTools` allowlist. Inline comment in runScheduled.ts must cite Pitfall 5 to anchor the constraint. |
| T-7-10 | Tampering | event.data.jobId could be a UUID injection / unexpected type | mitigate | Handler extracts `jobId` as a string and the insert SQL uses parameterized values via the existing drizzle-orm template (no string concat); same SQL pattern as 0002_pg_cron.sql which already uses `format(%L)` for safe quoting. |
</threat_model>

<verification>
- packages/inngest workspace exists and typechecks
- runScheduledAgent is defined with retries=3 and narrowed sandbox
- inngest.test.ts asserts function metadata + (with DB) the DB-write path
- Pitfall 5 cited in runScheduled.ts inline comment
- Pitfall 4 honored in client.ts (no fake signing key in dev)
</verification>

<success_criteria>
- @agent-os/inngest package builds and typechecks
- runScheduledAgent function exported and unit-tested
- Sandbox narrowed (allowedTools / concurrency)
- Function ready for Hono mount in Plan 04
</success_criteria>

<output>
Create `.planning/phases/07-tools-registry-inngest-scheduler-browserbase-tool-browser/07-03-SUMMARY.md` when done. Summary must list package contents, function id, narrowed-sandbox decisions, and confirm Pitfalls 4 and 5 are honored in code.
</output>
