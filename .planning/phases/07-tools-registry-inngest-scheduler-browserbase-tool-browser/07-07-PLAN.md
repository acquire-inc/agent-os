---
phase: 07-tools-registry-inngest-scheduler-browserbase-tool-browser
plan: 07
type: execute
wave: 3
depends_on:
  - 07-02
  - 07-05
files_modified:
  - scripts/seed/acqu-tool-browser.ts
  - scripts/seed/seed-tool-browser.ts
  - package.json
  - packages/core/src/architect/architect.test.ts
autonomous: true
requirements:
  - SC-7-1
  - SC-7-3
user_setup: []

must_haves:
  truths:
    - "scripts/seed/acqu-tool-browser.ts seeds the tool.browser row in the tools registry for tenant Acqu."
    - "Re-running pnpm seed:tool-browser is idempotent (no duplicate row)."
    - "tool.browser row carries requires_approval=true (safety default) and kind='custom'."
    - "Architect tests continue to pass (regression gate — Phase 3/4 must remain green)."
    - "No agent is bound to tool.browser in this plan — binding deferred to its first real consumer per RESEARCH Open Question 1."
  artifacts:
    - path: "scripts/seed/acqu-tool-browser.ts"
      provides: "Seed script that calls ensureTool for the tool.browser row"
      contains: "tool.browser"
    - path: "scripts/seed/seed-tool-browser.ts"
      provides: "Thin batch wrapper invoked by pnpm seed:tool-browser"
      contains: "tool-browser"
    - path: "package.json"
      provides: "pnpm script `seed:tool-browser`"
      contains: "seed:tool-browser"
  key_links:
    - from: "pnpm seed:tool-browser"
      to: "tools table row {key:'tool.browser'}"
      via: "ensureTool(db, TENANT_IDS.acqu, ...)"
      pattern: "tool.browser"
    - from: "BrowserToolInputSchema (Plan 05)"
      to: "tools.input_schema column (jsonb)"
      via: "zodToJsonSchema serialization at seed time"
      pattern: "input_schema"
---

<objective>
Seed the `tool.browser` row in the `tools` registry for tenant Acqu — proving the registry round-trips end-to-end. Per RESEARCH Open Question 1, do NOT bind to any agent yet; binding is deferred to the first real consumer in Phase 8+. Also extend the existing architect test to assert the architect blueprint surface remains compatible with the new tools field (regression gate — Phase 3/4 must stay green).

Purpose: P7-SC1 ("tools table + agent_tools join") and P7-SC3 ("tool.browser registered") both demand a real row exists. This plan plants that row using the canonical seed-script shape used by every other Acqu agent seed.
Output: A seeded `tool.browser` row + a script callable via `pnpm seed:tool-browser` + a regression assertion in the architect test.
</objective>

<execution_context>
@/home/user/agent-os/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/agent-os/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-tools-registry-inngest-scheduler-browserbase-tool-browser/07-RESEARCH.md
@.planning/phases/07-tools-registry-inngest-scheduler-browserbase-tool-browser/07-PATTERNS.md
@CLAUDE.md
@scripts/seed/acqu-vitals.ts
@packages/core/src/seed/seedAgent.ts
@packages/tool-browser/src/types.ts

<interfaces>
<!-- Extracted contracts the executor needs. -->

From scripts/seed/acqu-vitals.ts (canonical seed shape):
- Standard header: `import { createDb, schema } from "@agent-os/db"; import { TENANT_IDS } from "@agent-os/shared"; ...`
- main() guard: `if (import.meta.url === \`file://${process.argv[1]}\`) { main().catch((err) => { console.error(err); process.exit(1); }); }`
- process.exit(0) on success

From packages/core/src/seed/seedAgent.ts (Plan 02 output — ensureTool):
- `export async function ensureTool(db, tenantId, args: { key, name, kind?, inputSchema?, requiresApproval?, reversible? })`

From packages/tool-browser/src/types.ts (Plan 05):
- `BrowserToolInputSchema` zod schema; serialize to JSON Schema at seed time (use zod's built-in `z.toJSONSchema()` if available, or `zod-to-json-schema` package — fall back to `{ type: 'object', properties: { url: { type: 'string', format: 'uri' }, instruction: { type: 'string', minLength: 1 } }, required: ['url','instruction'] }` literal if no helper available, to avoid adding a new dep)

From RESEARCH Open Question 1:
- DO NOT bind tool.browser to any agent in this phase. The first real consumer (Phase 8+) adds the binding via its own AgentSpec.tools field.

From packages/core/src/architect/architect.test.ts:
- The architect test is the standing regression gate for blueprint parsing + hydration; any change to AgentSpec/Bundle must not break it. The existing `test:architect` script in packages/core/package.json runs it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author scripts/seed/acqu-tool-browser.ts</name>
  <files>scripts/seed/acqu-tool-browser.ts</files>
  <read_first>
    - scripts/seed/acqu-vitals.ts (canonical seed structure — `main()`, import.meta guard, process.exit, console.log summary)
    - packages/core/src/seed/seedAgent.ts (ensureTool helper signature)
    - packages/tool-browser/src/types.ts (BrowserToolInputSchema — to derive input_schema for the row)
    - .planning/phases/07-tools-registry-inngest-scheduler-browserbase-tool-browser/07-PATTERNS.md (L442-L479 — seed-tool-browser pattern)
    - .planning/phases/07-tools-registry-inngest-scheduler-browserbase-tool-browser/07-RESEARCH.md (Open Question 1 — do NOT bind to any agent)
  </read_first>
  <behavior>
    - Tsx-runnable script with import.meta guard + main()
    - Calls `await ensureTool(db, TENANT_IDS.acqu, { key: 'tool.browser', name: 'Browser', kind: 'custom', description: 'Headless browser automation via Browserbase + Stagehand', inputSchema: <serialized BrowserToolInputSchema>, requiresApproval: true, reversible: false })`
    - Re-running the script is a no-op (ensureTool is idempotent per (tenantId, key))
    - Console output: `Seeded tool: tool.browser (id=...) for tenant acqu`
    - DOES NOT call bindTool for any agent (per Open Question 1)
    - Exits 0 on success; non-zero on DB error or missing DATABASE_URL
  </behavior>
  <action>
    Create scripts/seed/acqu-tool-browser.ts using the exact shape of scripts/seed/acqu-vitals.ts (PATTERNS.md L442-L479). Imports: `createDb, schema` from `@agent-os/db`, `TENANT_IDS` from `@agent-os/shared`, `ensureTool` from `@agent-os/core` (specifically from the seedAgent module — check the existing core exports; if ensureTool isn't re-exported from `@agent-os/core` root, add the re-export in packages/core/src/index.ts as part of this task). main() body: validate DATABASE_URL or throw; createDb; const tool = await ensureTool(db, TENANT_IDS.acqu, { key: 'tool.browser', name: 'Browser', kind: 'custom', description: 'Headless browser automation via Browserbase + Stagehand', inputSchema: <input_schema literal>, requiresApproval: true, reversible: false }); console.log(`Seeded tool: ${tool.key} (id=${tool.id}) for tenant acqu`); process.exit(0). For inputSchema: use a hand-written JSON Schema literal `{ type: 'object', properties: { url: { type: 'string', format: 'uri' }, instruction: { type: 'string', minLength: 1 }, extractSchema: { type: 'object' } }, required: ['url', 'instruction'] }` — avoids adding a zod-to-json-schema dep just for one row. DO NOT call bindTool for any agent (RESEARCH Open Question 1: defer to first real consumer in Phase 8). Use the import.meta.url guard pattern at the bottom: `if (import.meta.url === \`file://${process.argv[1]}\`) { main().catch((err) => { console.error(err); process.exit(1); }); }`.
  </action>
  <verify>
    <automated>test -f scripts/seed/acqu-tool-browser.ts && grep -q "tool.browser" scripts/seed/acqu-tool-browser.ts && grep -q "ensureTool" scripts/seed/acqu-tool-browser.ts && ! grep -q "bindTool" scripts/seed/acqu-tool-browser.ts</automated>
    <human-check>Run `DATABASE_URL=$LOCAL_DB pnpm tsx scripts/seed/acqu-tool-browser.ts` and confirm console prints `Seeded tool: tool.browser (id=...)`. Re-run and confirm the same id is returned (idempotent — Plan 02 ensureTool semantics).</human-check>
  </verify>
  <acceptance_criteria>
    - Source: file exists with import.meta guard + main()
    - Source: file calls `ensureTool` with `key: 'tool.browser'`
    - Source: file sets `requiresApproval: true` (deny-by-default safety)
    - Source: file does NOT call `bindTool` (Open Question 1)
    - Behavior: running the script writes one row to the tools table; re-running returns the same row
  </acceptance_criteria>
  <done>tool.browser row seeded; no agent binding (deferred to Phase 8).</done>
</task>

<task type="auto">
  <name>Task 2: Add seed:tool-browser pnpm script + batch wrapper</name>
  <files>scripts/seed/seed-tool-browser.ts, package.json</files>
  <read_first>
    - package.json (root — find existing `seed:phase-1` script to mirror)
    - scripts/seed/ (look at any existing `seed-phase-N.ts` batch runner for the pattern)
    - scripts/seed/acqu-tool-browser.ts (the seed you just wrote)
  </read_first>
  <behavior>
    - `scripts/seed/seed-tool-browser.ts`: thin wrapper that imports and calls the main from acqu-tool-browser.ts (or re-runs the same logic — keep it simple, can be a 5-line file)
    - Root `package.json` adds `"seed:tool-browser": "tsx scripts/seed/seed-tool-browser.ts"` (or directly to acqu-tool-browser.ts — pick whichever matches the existing `seed:phase-1` pattern)
    - Script invokable via `pnpm seed:tool-browser` from repo root
  </behavior>
  <action>
    Inspect root package.json for how existing seeds are wired (`pnpm seed:phase-1`, `pnpm seed:phase-2`, etc.) — match that exact pattern. If the existing pattern uses a per-batch wrapper script (e.g. `scripts/seed/seed-phase-1.ts` that imports and runs all phase-1 seeds), create `scripts/seed/seed-tool-browser.ts` as a 5-line wrapper that imports the seed function from acqu-tool-browser.ts and calls it. If the existing pattern points pnpm scripts directly at the acqu-*.ts file, skip the wrapper and add `"seed:tool-browser": "tsx scripts/seed/acqu-tool-browser.ts"` to package.json directly. Either way, the user-facing command is `pnpm seed:tool-browser`. Confirm: `pnpm seed:tool-browser --help` or `pnpm run | grep seed:tool-browser` shows the script.
  </action>
  <verify>
    <automated>grep -q "seed:tool-browser" package.json</automated>
    <human-check>Run `DATABASE_URL=$LOCAL_DB pnpm seed:tool-browser` and confirm the row is seeded (or already-present message); re-run and confirm idempotent.</human-check>
  </verify>
  <acceptance_criteria>
    - Source: root `package.json` has `"seed:tool-browser"` in scripts
    - CLI: `pnpm run | grep -q seed:tool-browser` exits 0
    - Behavior: command seeds the row idempotently
  </acceptance_criteria>
  <done>pnpm seed:tool-browser is the canonical entry point.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend architect.test.ts with tools-field regression assertions (P7-SC1 regression gate)</name>
  <files>packages/core/src/architect/architect.test.ts</files>
  <read_first>
    - packages/core/src/architect/architect.test.ts (existing tests — find the AgentSpec / hydrate / blueprint shape assertions)
    - packages/core/src/seed/seedAgent.ts (the AgentSpec.tools optional field added in Plan 02)
    - .planning/phases/07-tools-registry-inngest-scheduler-browserbase-tool-browser/07-VALIDATION.md (task 7-07 — extend architect test, file already exists)
  </read_first>
  <behavior>
    - Adds a new assertion section `[architect tools compat]` (or similar) to the existing test file
    - Asserts: an architect-produced blueprint that does NOT include `tools` still hydrates/seeds (compat — same as Plan 02's seedAgent.test.ts assertion, but from the architect surface)
    - Asserts: if architect surfaces unknown tool keys (tools not in the registry), the hydrate step either clamps them out or surfaces a clear warning (per VALIDATION.md task 7-07 "hydrate clamps unknown")
    - Existing architect assertions remain untouched
  </behavior>
  <action>
    Append a new section to packages/core/src/architect/architect.test.ts that exercises the architect's behavior under the new tools field. First, find the existing tests that build a Blueprint or AgentSpec — mirror their construction style. Test 1: build an architect Blueprint that has agents WITHOUT a tools field; assert hydrate produces an AgentSpec where `spec.tools` is undefined or `[]` (compat with the optional `?:`). Test 2: build a Blueprint with `agents: [{ ..., tools: [{ key: 'tool.nonexistent', name: 'Fake' }] }]`; assert that the hydrate path EITHER clamps the unknown tool out of the final spec OR returns a warning surfaced to the caller (check the existing architect hydrate code to see which behavior is the convention — mirror what `mcpNames` does when an MCP isn't found; e.g. findMcpByName throws). Pick the behavior that matches the existing convention and assert against it. Per VALIDATION.md task 7-07: "Architect blueprint can list tools; hydrate clamps unknown" — the goal is to prove the architect doesn't crash on unknown tools and that the registry boundary is respected.
  </action>
  <verify>
    <automated>grep -q "tools" packages/core/src/architect/architect.test.ts && cd /home/user/agent-os && pnpm --filter @agent-os/core run test:architect</automated>
  </verify>
  <acceptance_criteria>
    - Source: architect.test.ts asserts an architect blueprint without `tools` field still hydrates
    - Source: architect.test.ts asserts unknown tool keys are either clamped or surfaced as warnings (matching existing mcpNames convention)
    - Test: `pnpm --filter @agent-os/core run test:architect` exits 0 (existing tests + new assertions all green)
  </acceptance_criteria>
  <done>P7-SC1 regression gate covered. Phase 3/4 architect functionality verified intact under the new tools surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Seed script → DB | Same as Plan 02 — seeds run as superuser; tenant logic is in script code (ensureTool always uses TENANT_IDS.acqu) |
| Architect Blueprint → AgentSpec | LLM-generated blueprints could include arbitrary tool keys; hydrate must clamp |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-22 | EoP | Architect surfaces unknown tool keys → unintended bindings | mitigate | Hydrate path either clamps unknown tools or throws (mirror findMcpByName convention). Asserted by architect.test.ts new test. |
| T-7-23 | Tampering | input_schema JSON in the seeded row diverges from BrowserToolInputSchema (Plan 05) | mitigate | Hand-written JSON Schema literal in acqu-tool-browser.ts mirrors the zod shape. Runtime validation in runBrowserTool (Plan 05) is the source of truth — DB column is informational metadata. Documented in SUMMARY. |
| T-7-24 | EoP | tool.browser bound to wrong agent at seed time | mitigate | Open Question 1: this plan does NOT bind to any agent. Test 1 in Task 1 asserts no bindTool call. First binding deferred to Phase 8+ consumer. |
</threat_model>

<verification>
- scripts/seed/acqu-tool-browser.ts exists and is idempotent
- pnpm seed:tool-browser command available and functional
- No agent binding created (defer to Phase 8+)
- Architect regression test extended; existing tests remain green
</verification>

<success_criteria>
- tool.browser row exists in tools table for tenant Acqu
- requires_approval=true on the seeded row (safety default)
- Seed command idempotent
- Architect tests still pass (Phase 3/4 unbroken)
</success_criteria>

<output>
Create `.planning/phases/07-tools-registry-inngest-scheduler-browserbase-tool-browser/07-07-SUMMARY.md` when done. Summary must record: row id (or "idempotent — already present"), confirm no agent binding was created, list the architect test additions, and note the input_schema literal vs zod-schema source-of-truth split.
</output>
